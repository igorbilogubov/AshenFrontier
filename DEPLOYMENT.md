# Hetzner: общая 3D-опушка

## Следующая версия аккаунтов — ещё не опубликована

Код Google-входа и schema 5 подготовлен локально. Перед его выкладкой настроить OAuth Web Client и точный callback `/auth/google/callback`; в закрытое окружение Compose добавить `GAME_PUBLIC_ORIGIN`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`. Compose требует эти значения. Docker build включает `auth/`; `.dockerignore` явно пропускает этот каталог, а `npm run check` сопоставляет все локальные `COPY` из Dockerfile с allowlist контекста. [Настройка](docs/GOOGLE_AUTH_SETUP.md).

Schema 5 удаляет старый столбец ключей героев; существующие строки остаются без аккаунта, случайное автоматическое присвоение запрещено. Необходимо закрыть вопрос о переносе нынешних тестовых героев до переключения пользовательской базы. После миграции откат только контейнера к protocol 2 несовместим: требуется совместимый релиз. Общая база и production в текущей задаче не менялись; новый push/deployment не выполнялись. Текущее исключение пользователя о новых backups/проверках сохранности остаётся в силе, volume и старые архивы не удалять.

Проверено 16 сентября 2026. Текущий релиз: **20260916-consumables-afk-01**, runtime commit `7b5a7b7a3889eb0dcbc2cbcfc62efd8416f4b48c`, образ `ashen-frontier:20260916-consumables-afk-01`, игра и PostgreSQL healthy, схема БД 4. Публичная игра: https://ashen-frontier.91.99.21.123.sslip.io/ ; состояние: https://ashen-frontier.91.99.21.123.sslip.io/health .

Это последняя проверенная выкладка, а не автоматический указатель на HEAD репозитория. Приватный GitHub: [igorbilogubov/AshenFrontier](https://github.com/igorbilogubov/AshenFrontier). Подготовка GitHub/документов не требует перезапуска игры. Workflow CI выполняет только проверки; секретов SSH и данных героев в GitHub нет. Перед следующей выкладкой перечитать живые `current`, image и health.

## Зелья в рюкзаке и неподвижный AFK: 20260916-consumables-afk-01

По команде пользователя выполнены обновление Markdown, commit, push `main` и deployment. В пакет входят стопки зелий в общих ячейках рюкзака, назначения Q/W и счётчики, PostgreSQL schema 4, неподвижный AFK в любой точке, исправленная шестерёнка настроек, маленькие значки добычи, продажа привязанных вещей и правильные пакеты ручных навыков.

- `npm run check` — 57 модулей. Чистый runtime `7b5a7b7` собран отдельно, пока прежний сервер работал. Полный прогон в новом образе на отдельной временной PostgreSQL 18.6 — **212/212**, без пропусков, около 78 секунд. [CI runtime-коммита](https://github.com/igorbilogubov/AshenFrontier/actions/runs/35061101790) успешен.
- По прямому указанию пользователя **резервная копия, восстановление дампа и проверка сохранности production-героев пропущены**. Это актуальное исключение для нынешнего тестового этапа; сравнение героев этой выкладкой не подтверждается. Старые копии и существующее расписание backup не изменялись.
- Штатно остановлен только игровой процесс (ExitCode 0), затем выполнен Compose `up -d --no-deps --no-build --wait game` с новым `ASHEN_RELEASE` и прежним env-файлом. Новая схема 3→4 применена при запуске. PostgreSQL-контейнер/volume и закрытые параметры не заменялись, локальные данные не передавались.
- Публичные HTTP/WSS работают: существующий QA-ключ получил авторизованный снимок мира с `consumableInventory`, `quickSlots`, `afkRadius` и 71 мобом леса, WebSocket ping/pong успешен. Это проверка подключения/контракта без исторического сравнения прогресса.
- **21** публичный HTML/JS/CSS/SVG/GLB совпал побайтно с проверенной сборкой; семь закрытых путей/стресс-API возвращают 404. Новая визуальная сессия в production не проводилась: этот же клиент проверен в браузере локально на предыдущем этапе, см. QA.
- Caddyfile и ID/время запуска всех **10** остальных контейнеров, включая PostgreSQL, не изменились. Игра и шесть соседних HTTPS-адресов отвечают 200. После проверок `current` атомарно переключён на новый релиз, `source-commit.txt`/`release.json` прочитаны обратно. Старый `20260915-camp-afk-01` оставлен.

Артефакты: `artifacts/deploy-20260916-consumables-afk-01/`, исключены из Git. Последующий коммит документации не меняет runtime SHA. Возврат к прежнему коду schema 3 после появления стеков не является совместимым откатом: исправлять совместимым релизом, не подменять базу старым дампом. Длительный AFK, экономика и вместимость сервера этой выкладкой не измерялись.

## Лагерь, медведи, сундук, AFK и интерфейс: 20260915-camp-afk-01

По команде пользователя выполнены commit, push `main` и deployment. Релиз включает исправления попаданий и преследования, 71 моба в лесу и 24 в четырёх загонах Стадиума, медведей, открытый дом с личным сундуком, ограду безопасной зоны, зелья HP/MP, навыки 1–4, сохраняемые настройки AFK, обновлённые HUD/чат, курсор и панель выбранной цели.

- `npm run check` проверил 54 модуля; локальные тесты и полный прогон в новом runtime-образе с изолированным PostgreSQL 18.6 — **194/194**, без пропусков. [GitHub CI runtime commit](https://github.com/igorbilogubov/AshenFrontier/actions/runs/35010892589) успешен. Production не использовался для нагрузки.
- После штатной остановки только игры (ExitCode 0) снят `/opt/ashen-frontier/backups/postgres-20260915T190536118602Z.dump`, права 0600. Дамп восстановлен в отдельную временную БД и мигрирован 1→3. До и после миграции совпали количество и хеши всех прежних колонок шести таблиц: 5 героев, 17 экземпляров и позиций предметов, 18 записей идентичности, 14 бросков, 3440 операций.
- Та же проверка выполнена для рабочей базы после миграций 2/3 и до запуска игры: старые данные не изменились. Сохранены PostgreSQL-контейнер, volume, пароли и роль `ashen_game`. Затем запущен только `game` через Compose `--no-deps --no-build --wait`. После старта живые игроки переподключились; дальнейшее полное равенство таблиц не предполагается.
- Два прежних QA-героя восстановили 18 полей постоянного прогресса через публичный WSS. Проверены взаимная видимость, лес → Стадиум → лес (71/24 моба), сохранённая позиция Стадиума, перенос прежнего доспеха в сундук и обратно, сохранение сундука и настроек AFK после переподключения. Проверочный доспех снова надет, настройки возвращены к исходным.
- Все 16 проверенных публичных HTML/JS/CSS/SVG/GLB совпадают побайтно с проверенной сборкой; семь закрытых путей/стресс-API возвращают 404. Визуальные сценарии этого же клиента выполнены локально и описаны в QA.md; эта выкладка отдельно проверена по HTTP/WSS, без новой полной визуальной сессии в production.
- Caddyfile, ID/время запуска девяти соседних контейнеров и PostgreSQL не изменились. Игра и шесть соседних HTTPS-адресов отвечают 200. `current` атомарно переключён на `/opt/ashen-frontier/releases/20260915-camp-afk-01`, `source-commit.txt` и `release.json` прочитаны обратно. Предыдущий `20260915-skills-stadium-01` сохранён.

Отчёты и закрытые QA-данные: `artifacts/deploy-20260915-camp-afk-01/`, исключены из Git. Последующий коммит документации не меняет runtime SHA. Схема 3 и новые предметы в сундуке несовместимы со старым кодом схемы 1: для исправления выпускать совместимый релиз; не запускать прежний образ и не восстанавливать старый дамп поверх нового прогресса. Длительный AFK, экономика и вместимость под нагрузкой этой выкладкой не подтверждены.

## История: четыре навыка и Стадиум 20260915-skills-stadium-01

По отдельной команде пользователя выполнен deployment локального `main`: по четыре навыка Q/E/Z/X для каждого класса, обновлённые движения/эффекты, портал лагеря и отдельный Стадиум с тремя загонами по шесть мобов. Runtime commit `c601124` включает пакет `9baf87e` и исправление переключения тестовых AFK-сценариев. GitHub push в этой задаче не выполнялся.

- Новый образ собран отдельно; после исправления гонки тестового стенда `npm run check` и 162/162 теста прошли локально, все 162/162 — в runtime-образе с изолированным PostgreSQL 18.6, без пропусков. Первый серверный прогон 161/162 и причина сохранены в QA; рабочий мир для нагрузки не использовался.
- После штатной остановки только игры создан `/opt/ashen-frontier/backups/postgres-20260915T172321671556Z.dump` (0600). Дамп восстановлен в отдельную временную БД: количества и хеши строк всех семи таблиц совпали. В исходнике четыре героя и восемь предметов. Уже при запуске игрок переподключился, поэтому полные строки героев/журнал штатно изменились; отдельно подтверждены все 19 полей постоянного прогресса четырёх героев и неизменность предметных таблиц. Локальные данные не переносились.
- SQL-миграции не требуются: schemaVersion=1, сохранения V3, координаты Стадиума и cooldown новых навыков поддерживаются прежними полями. Volume, пароли и контейнер БД сохранены. Compose выполнен с `--no-deps`: относительный путь postgres-init.sh в новом каталоге не вызвал пересоздание базы.
- По публичному WSS два существующих QA-героя восстановили 26 полей; проверены взаимная видимость, переход лес → Стадиум → лес, 41/18 мобов по областям и сохранённая позиция Стадиума после переподключения. В браузере проверочный лучник загрузил новый HUD и прошёл портал; ошибки/предупреждения консоли отсутствовали. Все 12 проверенных публичных HTML/JS/CSS/GLB совпали со сборкой, закрытые пути и стресс-API возвращают 404.
- Caddyfile и ID/время запуска всех девяти соседних контейнеров, а также PostgreSQL, не изменились. Шесть соседних HTTPS-адресов и игра отвечают 200. `current` атомарно переключён на новый каталог после проверок; предыдущий `20260915-pg-01` оставлен.

Отчёты: `artifacts/deploy-20260915-skills-stadium-01/`, не входят в Git. Секреты QA и дампы закрыты. На сервере `source-commit.txt` и `release.json` фиксируют runtime SHA; отдельный последующий коммит документации не меняет работающий образ. Проверка не подтверждает длительную вместимость или баланс PvP. Предыдущий PG-код не понимает координаты Стадиума: не выполнять слепой откат и не восстанавливать старый дамп поверх нового прогресса; предпочтителен совместимый исправленный релиз.

## История: PostgreSQL и открытая карта 20260915-pg-01

По команде пользователя выполнены push и deployment: PostgreSQL, большая карта с четырьмя спотами и 41 мобом, наземная добыча, торговец, обновлённый инвентарь/HUD, хват лука и удержание ПКМ. В релизе два навыка на класс; последующий запрос четырёх навыков и Стадиума находится в отдельных ветках и этим deployment ещё не опубликован.

- `current` → `/opt/ashen-frontier/releases/20260915-pg-01`; предыдущий `20260915-ts-01` сохранён. Runtime-код собран из `4e1bb18`; изменения до release commit `96374e9` добавляют backup-утилиту и исправляют повтор подключения теста, код приложения между ними идентичен.
- PostgreSQL 18.6: сервис `database`, контейнер `ashen-frontier-database-1`, volume `ashen-frontier_postgres-data`, внутренняя сеть `ashen-frontier_database`, порт 5432 на хост не опубликован. Игра подключается отдельной ролью `ashen_game`, schemaVersion=1, writer=true. Пароли созданы на VPS в `/opt/ashen-frontier/ops/database.env` (0600); не менять их простым редактированием env у существующего volume.
- По ранее принятому решению 16 старых JSON-героев **не импортированы**. После остановки старой игры сохранён `/opt/ashen-frontier/backups/heroes-before-20260915-pg-01.json` (0600, UID 1000), исходный JSON не заменён локальным. Новые PostgreSQL-герои при следующих выкладках сохраняются.
- GitHub CI релизного SHA успешен; в runtime-образе с отдельным временным PostgreSQL прошли **137/137** тестов. Первый серверный прогон выявил тест, который считал временный отказ входа при записи окончательным; helper теперь повторяет только `storage_unavailable`, как браузер. Повтор полный, без пропусков.
- По публичному WSS два QA-героя видят друг друга и движение. После штатного рестарта 26 полей каждого, включая вещи, статы, XP и золото, совпали. 11 публичных JS/CSS/HTML/GLB совпали с проверенной сборкой. Chrome загрузил проверочного лучника, карту, торговца и HUD без ошибок консоли; тестовая вкладка закрыта.
- Новый custom-format dump восстановлен в **отдельную временную БД**; совпали число и контрольные суммы строк всех семи таблиц. Источник содержал двух QA-героев, четыре стартовых предмета, восемь записей журнала; таблица случайных бросков была пустой. Дамп `/opt/ashen-frontier/backups/postgres-20260915T155354412620Z.dump`, права 0600. Случайные броски отдельно проверены локальными/сетевыми тестами.
- Ежедневная PostgreSQL-копия в 03:35 UTC: `/etc/cron.d/ashen-frontier-postgres-backup`, скрипт `current/deploy/backup-postgres.py`; журнал приватный, logrotate настроен. Старый JSON-cron архивирован в `ops/legacy-json-backup.cron`. Будущее срабатывание cron ещё не наблюдалось. [Резервирование](docs/POSTGRES_BACKUP.md).
- Caddyfile и ID/время запуска всех девяти соседних контейнеров не менялись. Все шесть соседних HTTPS-адресов и игра отвечают 200. На короткой проверке без нагрузки игра использовала около 25 MiB/256 MiB, БД 46 MiB/384 MiB; это не подтверждение вместимости.

Отчёты исключены из Git: `artifacts/deploy-20260915-pg-01/` (before/after, public-assets, verification, backup-restore, online-check); ключи QA не публикуются. Копии на том же VPS не защищают от потери всего хоста.

### Следующий PostgreSQL-релиз

Собирать отдельный каталог/образ из чистого проверенного commit. Перед переключением выполнить `python3 /opt/ashen-frontier/current/deploy/backup-postgres.py`. Использовать прежний проект/volume/пароли, не выполнять `down -v`. Compose запускать с `--env-file /opt/ashen-frontier/ops/database.env` и явно заданным `ASHEN_RELEASE=<новый релиз>`, поскольку env содержит предыдущий тег. Проверить миграции отдельно, затем штатно остановить только игру и выполнить `up -d --no-deps --no-build --wait game`. После HTTP/WSS/сохранений атомарно обновить `current`, проверить соседей. Прежний JSON-образ не читает PostgreSQL: автоматического отката данных назад в JSON нет, для исправлений предпочтителен совместимый PG-релиз. Текущую БД перед откатом всегда архивировать.

## Историческая инфраструктура JSON-релиза

Следующие разделы описывают `20260915-ts-01` и более ранние релизы. Источником новых сохранений теперь служит PostgreSQL; прежние JSON-команды не являются инструкцией обновления текущего релиза.

## Размещение

SSH: `root@91.99.21.123`, хост `ubuntu-4gb-nbg1-1`. Не подменять его другим доступным сервером или SSH-алиасом.

| Объект | Значение |
| --- | --- |
| Текущий релиз | `/opt/ashen-frontier/current` → `/opt/ashen-frontier/releases/20260915-ts-01` |
| Compose / контейнер | проект `ashen-frontier`, сервис `game`, контейнер `ashen-frontier` |
| Код релиза | `/opt/ashen-frontier/current/deploy/compose.yaml` |
| Постоянные герои | `/opt/ashen-frontier/data/heroes.json` |
| Защитная копия перед 3D | `/opt/ashen-frontier/backups/heroes-before-20260914-3d-01.json` |
| Ежедневные копии | `/opt/ashen-frontier/backups`, скрипт `/opt/ashen-frontier/ops/backup.py`, расписание `/etc/cron.d/ashen-frontier-backup` |
| Общий прокси | контейнер `n8n-caddy-1`, файл `/opt/n8n/Caddyfile`, сеть `n8n_default` |

Приложение работает на Node 24 под непривилегированным пользователем, с read-only файловой системой и отдельным каталогом данных. Ограничения: 256 MiB, 0,5 CPU, 64 процесса, журналы 3 × 10 MB. Порт 4731 доступен внутри Docker; браузер использует HTTPS/WSS через существующий Caddy. В ходе этого обновления Caddy, firewall и остальные сервисы не менялись.

Серверный файл героев — единственный источник актуального онлайн-прогресса. **Не копировать поверх него локальный data/heroes.json.** Файлы героев содержат секретные ключи. Копии находятся на том же VPS; внешнее резервирование и восстановление после потери VPS остаются отдельной работой.

## История: что подтверждено при первом общем 3D-релизе

- В новом production-образе прошли 16 серверных/сетевых тестов с временными данными; весь локальный набор содержит 27 тестов, включая GLB и анимации.
- До переключения выполнена миграция всех 11 прежних героев в памяти: сохранность каждого уровня, опыта, золота, предмета и надетой экипировки проверена без записи в production.
- Старый игровой контейнер штатно остановлен; сделана отдельная копия актуального JSON с правами 0600. Новый контейнер использует тот же постоянный каталог.
- HTTPS/WSS: два новых QA-героя видят друг друга и одно движение. После контрольного рестарта контейнера строго совпали их id, класс, уровень, опыт, золото, вещи, экипировка, координаты и задача.
- Реальный браузер загрузил модели героя и мобов с публичного адреса, вошёл в мир и автоматически восстановил соединение после рестарта; ошибок рендера не обнаружено.
- Все 11 исходных записей по существенным полям сохранили контрольные суммы. После QA добавились три тестовых героя.
- SHA-256 Caddyfile остался `52dc330ba02e8076e5e132c96056b6c7fde6842040e88760da2fdfeb36c8a4fc`. ID и время запуска всех девяти других контейнеров не изменились.
- До/после HTTP 200: `longbrio.com`, `www.longbrio.com`, `longbrio.91.99.21.123.sslip.io`, `healthspan.91.99.21.123.sslip.io`, `mypathfit.com`, `www.mypathfit.com`.
- На кратком замере с тестовым браузером игровой контейнер использовал около 21 MiB памяти. Это не нагрузочное испытание.

Локальные отчёты: `artifacts/shared-3d/remote-before.json`, `remote-after.json`, `verification.json`. `online-heroes.json` рядом содержит только секреты QA-профилей; каталог закрыт и исключён из Git. Его не отправлять в релиз.

## Проверки и обслуживание

```sh
npm run check
npm test
node scripts/smoke-online.mjs https://ashen-frontier.91.99.21.123.sslip.io
```

Последняя команда создаёт двух отдельных QA-героев и сохраняет их ключи в закрытый файл. После штатного рестарта только игры проверить тех же героев:

```sh
node scripts/smoke-online.mjs https://ashen-frontier.91.99.21.123.sslip.io --restore
```

На сервере:

```sh
docker inspect ashen-frontier --format '{{.State.Health.Status}}'
docker logs --tail 60 ashen-frontier
docker stats --no-stream ashen-frontier
python3 /opt/ashen-frontier/ops/backup.py
```

## Следующее обновление

Для параллельных задач сначала завершить и проверить согласованные изменения в общей папке на `main` по [WORKFLOW.md](WORKFLOW.md). Выкладывать чистый согласованный commit; записать его SHA рядом с идентификатором нового релиза и результатами проверки. Git push/merge сами по себе сервер не изменяют. Рабочий сервер не использовать как общую тестовую среду незавершённых веток.

Создать отдельный каталог `/opt/ashen-frontier/releases/<release>` и уникальный тег образа. После перехода на TypeScript передать `server.ts`, `world.ts`, загрузчик `server.mjs`, `shared/`, `stress/`, `storage/`, `auth/`, `public/`, `tsconfig.json`, `scripts/build.mjs`, package-файлы, Dockerfile и deploy-конфигурацию. Docker собирает `dist/` на отдельном этапе, в runtime устанавливаются только production-зависимости. `GAME_STRESS` в production запрещён; диагностические API не активируются. Не передавать локальные данные, node_modules, .env, артефакты или Blender-исходники. Собрать и проверить образ, пока прежняя игра работает. Затем штатно остановить только игру, сделать закрытую резервную копию актуальных данных и запустить новый образ:

```sh
ASHEN_RELEASE=<release> docker compose -f deploy/compose.yaml up -d --no-build --wait game
```

Проверить публичные HTTP/WSS, сохранения и другие сайты; только после успеха обновить ссылку current. Сохранить предыдущий образ/релиз и копию данных.

## Откат с 3D на прежний пиксельный сервер

Версии данных несовместимы по координатам и новым полям. Старый образ: `ashen-frontier:20260911-03`, каталог `/opt/ashen-frontier/releases/20260911-03`.

1. Штатно остановить только `ashen-frontier` и отдельно сохранить текущие версии 2; не уничтожать накопленный после перехода прогресс.
2. Для возврата именно к старому миру восстановить защищённую копию `heroes-before-20260914-3d-01.json` в каталог данных, сохранить владельца 1000:1000 и права 0600. Это откатывает изменения после перехода; выполнять только при принятом решении об откате данных.
3. Запустить предыдущий Compose с `ASHEN_RELEASE=20260911-03`, проверить его health/сохранения и вернуть current на предыдущий релиз.

Для обычного отката между совместимыми 3D-релизами сначала проверить совместимость схемы и по возможности сохранить новые данные. Не выполнять `docker compose down` в `/opt/n8n`, не заменять Caddyfile конфигурацией только игры и не перезапускать чужие контейнеры.

## Границы

Это общий тестовый 3D-мир с гостевым ключом героя и JSON-сохранениями. Полноценных аккаунтов, AFK, PvP/PK, рынка и платежей пока нет. Один ключ открывает героя только на своём сервере и только в одном соединении. Мобы и чат при рестарте создаются заново, личный прогресс сохраняется. Лимит 16 игроков пока не является подтверждённой вместимостью.

## Исправление патрулирования: 20260914-3d-02

Релиз меняет только серверную прогулку мобов: фиксированные достижимые точки, разгон, торможение и паузы. Модели, клипы, скорость погони и формат сохранений не менялись. 29 локальных тестов и 18 серверных тестов в новом образе прошли. После обновления два WSS-профиля восстановлены без изменения данных; открытый браузер пользователя снова показывает общий мир. Существующие 15 героев сохранены, Caddy и девять других контейнеров не менялись; шесть HTTPS-адресов отвечают 200. Отчёты: artifacts/patrol-fix/.

Перед остановкой сделана защищённая копия /opt/ashen-frontier/backups/heroes-before-20260914-3d-02.json. Для отката этого совместимого исправления использовать образ/Compose 20260914-3d-01 с тем же каталогом данных, без возврата старого файла героев.

## Доработка волка: 20260914-3d-03

Новая геометрия обычного волка, шаг/рысь и два поворотных клипа. Фаза походки привязана к перемещению после сетевого сглаживания, поворот патрулирующего волка ограничен 1,5 рад/с. В бестиарии есть переключатель «до / после»; старая модель загружается только для сравнения. Кабан, вожак, параметры боя и формат сохранений прежние.

31 локальный тест и все 31 тест в новом production-образе прошли. Для проверки героя к тестовому контейнеру отдельно подключён read-only отчёт сборки; в рабочий образ Blender-исходники не добавлялись. Публичные восемь обновлённых/контрольных ресурсов совпали с локальными байтами, код и GLB внутри работающего контейнера также сверены. Браузер загрузил новый бестиарий и воспроизвёл шаг без ошибок. Два прежних QA-профиля восстановились по WSS с тем же прогрессом.

Все 15 героев сохранили контрольные суммы основных полей прогресса; ID/время запуска девяти других контейнеров и Caddyfile не менялись, все шесть прежних HTTPS-адресов отвечают 200. Перед переключением создана копия `/opt/ashen-frontier/backups/heroes-before-20260914-3d-03.json`, права 0600, владелец 1000:1000. Для числового UID при создании копии используется Python `os.chown`; `install -o 1000` на этом хосте не принимает отсутствующее имя пользователя. При первой неудаче копирования защитный обработчик успешно вернул прежнюю игру.

Отчёты: `artifacts/wolf-polish/verification.json`, `public-assets.json`, `remote-tests.log`, `public-restore.log`. Откат: образ и Compose `20260914-3d-02` с тем же актуальным каталогом данных; старый JSON поверх новых сохранений не восстанавливать. Уже открытой игре нужно обновление страницы для загрузки новой модели.

## TypeScript, характеристики и новый HUD: 20260915-ts-01

По команде пользователя 15 сентября опубликован commit `8f9c93137cd7a9bb663539453e757ab9b089c0b8`: строгий TypeScript для клиента/сервера/правил, ручные характеристики и уровни, мана, окна персонажа и вещей, колбы HP/MP, удержание ЛКМ, неизменяемый класс и устранение дублирующих скелетов. Godot в рабочем релизе отсутствует. Стресс-стенд выключен в production, его API возвращает 404; нагрузочный прогон на рабочем мире не выполнялся.

Образ собран на VPS в два этапа, пока прежний контейнер работал. Все 59 тестов прошли внутри нового runtime-образа с временными данными и отдельно подключёнными read-only тестами/отчётом модели. GitHub CI того же SHA также завершился успешно. В рабочем контейнере нет dev-зависимостей или Blender-исходников; прежние лимиты, сеть, непривилегированный пользователь и read-only root сохранены.

До переключения проверена миграция всех 15 сохранений (девять legacy и шесть V2) в памяти: 25 прежних полей совпали с нормализацией старого сервера, V3 точно восстанавливается повторно, мана/бюджет очков выдаются штатно. После штатной остановки создана копия `/opt/ashen-frontier/backups/heroes-before-20260915-ts-01.json`, права 0600, UID 1000. Сервер использует прежний `/opt/ashen-frontier/data`; локальный файл героев не передавался. Схема V3 записывается при использовании героя, не все неактивные записи переписываются сразу.

После переключения и контрольного рестарта два прежних QA-профиля по публичному WSS восстановили 28 полей, включая V3, статы, ревизию, HP/MP, координаты и вещи; оба видят общий мир. У всех 15 исходных героев сохранены 13 основных полей прогресса. Для браузерной проверки создан один отдельный герой «Проверка релиза TS»: итоговый счётчик записей при проверке — 16. Браузер загрузил сцену, модели, колбы, окно четырёх характеристик и инвентарь с шестью слотами; переподключение после рестарта работает. Эти проверки не являются новым замером FPS или вместимости.

Двенадцать публичных ресурсов сверены побайтно с проверенной сборкой: HTML, JS, CSS и четыре GLB. Закрытые данные, TS/sourcemaps, диагностические API и прежний путь Godot дают 404. Caddyfile сохранил SHA-256 `52dc330ba02e8076e5e132c96056b6c7fde6842040e88760da2fdfeb36c8a4fc`; ID и время запуска девяти других контейнеров не изменились. Все шесть прежних HTTPS-адресов и игра отвечают 200. После этих проверок `current` атомарно переключён на новый релиз.

Отчёты: `artifacts/deploy-20260915-ts-01/` — `build.log`, `remote-tests.log`, `migration-audit.json`, `preservation.json`, `public-assets.json`, `v3-restore.log`, `before.json`, `after.json`. Каталог исключён из Git; состояние QA и ключи не публикуются.

### Откат этого релиза

Предыдущие образ и каталог `20260914-3d-03` сохранены, но V2-сервер не поддерживает распределённые статы и ману V3. Не считать откат образа автоматически совместимым с новыми сохранениями и не восстанавливать старый JSON поверх накопленного прогресса. Сначала штатно остановить игру и сохранить текущий V3 в отдельную защищённую копию. Предпочтителен исправленный V3-релиз; возврат к V2 требует явного решения о данных и проверки конвертации. Копия перед релизом служит точкой восстановления, её применение откатит прогресс после переключения. Остальные контейнеры и Caddy при откате не трогать.

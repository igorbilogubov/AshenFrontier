# Производительность: первый воспроизводимый профиль

15 сентября 2026. Строгий TypeScript перенесён и проверен; изменение языка само по себе не ускоряет игру. Профиль проведён на локальной ветке `codex/character-stats`, после миграции `33cac6e`. Production и Godot не участвовали.

## Вывод

Обычная сцена сейчас даёт около 29 FPS; полный лимит 16 сессий и бой — около 26–29 FPS. Стабильные 60 FPS ещё не достигнуты. Контрольный rAF с действующей сетью, но без работы игрового кадра даёт 59.8 FPS. Основная область для оптимизации — клиентский вывод сцены и анимированных моделей; локальный сервер укладывается в интервал 50 мс. Этот результат не обосновывает переход на Rust/Wasm.

Выявлена и устранена лишняя работа в клонах героя: `SkeletonUtils.clone` создавал 14 Skeleton для 14 skinned primitives с одинаковыми костями. Теперь части **одного** героя делят один скелет, разные герои сохраняют независимые кости и GPU-текстуры. Сравниваются также inverse bind matrices, bind matrices самих мешей остаются прежними. Уходящий герой освобождает GPU-текстуру скелета.

В сопоставимых прогонах 16 героев число зарегистрированных Three.js GPU-текстур снизилось с 164 до 64. Среднее время кода кадра уменьшилось примерно с 17.0–17.6 до 15.0–15.3 мс. Устойчивого прироста FPS в лагере это не дало: до 28.1–28.2, после 26.2–27.0. Уменьшение ресурсов подтверждено; обещать ускорение FPS по этому измерению нельзя. Геометрия и деформированные вершины всех семи клипов проверены тестами, внешний вид сохранён.

## Условия

- Apple M1, 8 CPU-ядер, 16 GiB RAM, питание от сети; macOS Darwin 25.6.0.
- Chrome 152, аппаратный WebGL2 через ANGLE/Metal. Окно игры 1527×810 CSS px, фактический DPR 1.10, обычные тени PCF 2048², стандартный фиксированный ракурс/зум.
- Один видимый браузерный клиент; остальные сессии — настоящие WebSocket-боты в том же локальном процессе Node 24.19.0. Это проверяет общую сцену и текущий протокол, а не 16 полноценных браузеров.
- Отдельный временный каталог и новые тестовые герои; уровни, урон и смерть настоящие. Положение/прогресс тестовых героев сбрасываются перед сценарием. Число мобов — 7; в бою живые/мёртвые и снаряды меняются, их число записано в отчёте.
- После появления всех моделей — 5 секунд прогрева, затем 12 секунд измерения. Первая серия — 10 основных + 3 контрольных прогона; повторная — 13. В каждой серии лагерь на 16 игроков повторён с исходной графикой.
- Godot-процессы остановлены. Другие приложения пользователя оставлены открытыми. На компьютере есть фоновая нагрузка и swap; это замер рабочего Mac, не чистая аппаратная лаборатория. В первом прогоне на 4 игроках было 19.4 FPS против 28.8 после: этот выброс нельзя честно приписать только исправлению скелетов.

## После исправления скелетов

CPU ниже — длительность кода внутри rAF, включая вызов драйвера; она не включает весь браузерный layout/compositing и не равна чистому времени процессора. p95 кадра — 95% интервалов не длиннее этого значения. Чем меньше, тем лучше.

| Сценарий | FPS | p95 кадра, мс | p99, мс | Кадров >50 мс | Код кадра, мс | p95 серверного тика, мс |
|---|---:|---:|---:|---:|---:|---:|
| 1 игрок · лагерь | 29.4 | 49.9 | 51.4 | 10 | 4.4 | 1.1 |
| 4 игрока · лагерь | 28.8 | 50.1 | 66.7 | 22 | 8.7 | 2.3 |
| 16 игроков · лагерь | 27.0 | 51.0 | 66.6 | 29 | 15.0 | 7.0 |
| 16 · без теней | 28.8 | 50.3 | 66.7 | 21 | 13.3 | 6.1 |
| 16 · без сэмплирования анимации | 24.1 | 51.4 | 283.3 | 35 | 15.4 | 6.8 |
| 16 · DPR 0.75 | 30.3 | 50.0 | 83.3 | 18 | 17.0 | 8.3 |
| 16 · без деревьев | 31.1 | 49.9 | 66.5 | 15 | 15.9 | 6.5 |
| 16 · обычная графика повтор | 26.2 | 51.4 | 84.2 | 23 | 15.3 | 6.9 |
| 16 · без вывода 3D (диагностика) | 42.4 | 34.7 | 50.6 | 6 | 6.4 | 7.7 |
| 16 · без подписей игроков | 27.4 | 51.4 | 66.1 | 38 | 15.1 | 6.8 |
| 16 · только rAF и сеть (контроль) | 59.8 | 18.3 | 18.6 | 0 | 0.0 | 6.9 |
| 16 · бой | 29.0 | 50.0 | 66.6 | 13 | 15.1 | 6.9 |
| 16 · бой и выбор моба каждый кадр | 29.0 | 50.0 | 68.2 | 15 | 18.2 | 6.9 |

## Что подтверждено и что остаётся гипотезой

**Клиентский рендер — существенная часть затрат.** В лагере 16 героев передаётся около 1.54 млн треугольников и 405 вызовов рисования за кадр, включая проход теней. Без вывода 3D, но с анимациями/DOM, получается 42.4 FPS; с одним rAF и сетью — 59.8. GPU-query доступен и не показал disjoint; обычная сцена на 16 героях — примерно 48–52 мс/query, DPR 0.75 и скрытые деревья — около 40–41. Эти времена нельзя складывать с CPU как независимые последовательные этапы или интерпретировать как процент загрузки GPU.

**Лес и разрешение влияют, но не объясняют всё.** 109 деревьев сейчас входят в крупные InstancedMesh: их листва содержит около 293 тыс. треугольников до теней, без отсечения отдельных деревьев. Диагностическое скрытие деревьев и уменьшение DPR дали около 30–31 FPS. Это не готовые настройки качества и не предложенный новый внешний вид.

**Отключение одних теней, подписей или сэмплирования клипов не дало стабильного выхода на 60 FPS.** Анимация — часть расходов, но переписывать её на Rust первым действием этот результат не оправдывает. Полный CPU/GPU-профиль драйвера и браузерного layout здесь не снят: запись разбита на участки игрового кода, GPU query и контрольные сценарии, а не DevTools trace.

**Выбор моба не доказан главным виновником.** Первый бой с проверкой raycast каждый кадр дал 18.9 FPS, повторный — 29.0. Состав живых мобов/боевые события не идентичны. Нельзя объявить raycast причиной этого разброса без отдельного фиксированного сценария с живыми целями.

**Локальный сервер не исчерпал 50 мс бюджета.** При 16 сессиях p95 полного тика обычно 6–8 мс, включая симуляцию и подготовку/отправку снимков. Сетевой генератор находится на том же M1, условия VPS с 0.5 CPU/256 MiB не воспроизведены. Это не доказательство длительной вместимости production.

Следующие ограниченные задачи: разделить лес на отсекаемые участки; ввести корректные общие границы видимости персонажей/мобов без обрезания анимаций; проверить детализацию и тени на дистанции. Каждый шаг сравнивать с исходной графикой при том же окне и числе акторов. Затем повторить на Windows с интегрированной графикой и провести длительный тест памяти/соединений. Массовое PvP/AFK, мобильный GPU, загрузка холодных ассетов и часы работы пока не измерены.

## Повторный запуск

```sh
npm ci
npm run check
npm test
npm run stress
# В браузере: http://127.0.0.1:4742/?stress=1&session=benchmark
# После завершения:
npm run stress:report
```

Нажать «Запустить весь тест» либо выбрать сценарий и «Проверить выбранный». Вкладку держать активной, не менять размер окна, зум и управление. Скрытие, потеря фокуса, ручное управление, смена числа игроков или потеря связи делают прогон недействительным. «Остановить» завершает текущий замер без записи действительного результата. После серии боты отключаются и тестовый наблюдатель возвращается в лагерь. Для окончания работы сервера — Ctrl+C. Новый запуск всегда создаёт другой временный мир; прежние игровые сохранения не используются.

`STRESS_PORT=4743 npm run stress` выбирает другой свободный порт. `/__stress/*` доступен только в явно включённом локальном стенде, проверяет loopback/Origin. Запуск режима на production, публичном host или с обычным каталогом героев отклоняется до чтения/изменения сохранений. На обычном сервере диагностические маршруты закрыты.

Исходные JSON с каждым необрезанным интервалом кадра, распределениями CPU/GPU, количеством сущностей/ресурсов, задержкой последнего снимка и серверными измерениями находятся в игнорируемом `artifacts/performance/`. `npm run stress:report` формирует сводный Markdown. В репозитории сохранён компактный числовой снимок всех 26 прогонов: [performance-2026-09-15.json](docs/performance-2026-09-15.json). Тестовые ключи/сохранения в отчёты не входят.


## Первая модульная экипировка — 15 сентября, локальная проверка

[Сводка замеров](docs/equipment-performance-2026-09-15.json). Изолированный `npm run stress`, временные сохранения, Chromium/M1, 1280×720, DPR 1; 5 секунд прогрева и 12 секунд измерения каждого сценария. Параметр `equipment=mixed-warrior` в адресе стенда создаёт воинов с десятью временными образцами и чередует лёгкий/тяжёлый комплект. `equipment=legacy-warrior` использует прежние стартовые вещи. Эти профили работают только у защищённого локального stress-контроллера.

| Профиль | Игроков | FPS | p95 кадра, мс | Средний код кадра, мс | Draw calls | Треугольники с тенями | p95 серверного тика, мс |
|---|---:|---:|---:|---:|---:|---:|---:|
| Смешанные новые вещи | 1 | 60,0 | 17,6 | 2,1 | 160 | 1 087 076 | 0,92 |
| Смешанные новые вещи | 4 | 60,0 | 17,6 | 2,8 | 253 | 1 169 562 | 2,10 |
| Смешанные новые вещи | 16 | 60,0 | 18,5 | 7,3 | 601 | 1 519 002 | 11,62 |
| Прежние вещи, тот же модульный GLB | 16 | 60,0 | 17,7 | 7,6 | 569 | 1 555 706 | 8,38 |

Во всех четырёх коротких сценариях не было кадров >50 мс. Контроль использует тот же новый ассет и сервер, поэтому не измеряет цену загрузки старого/нового бинарного файла; исторический замер выше имел другое окно/DPR и не подходит для прямого сравнения. Разницу 7,3/7,6 мс нельзя трактовать как доказанное ускорение. Частота ограничена 60 Гц.

Новый GLB около 4,8 МБ, 67 костей, все альтернативы суммарно 32 866 треугольников. Невидимые части не рисуются; у каждого героя одна палитра костей, геометрия/текстуры клонов общие. Количество draw calls выросло для украшений и отдельных деталей. Нагрузка при длительном переключении, память за часы, массовый бой и слабые устройства отдельно не измерены.


## Exo Gray / Erika Archer / Dreyar — 15 сентября, локальная проверка

[Числовой снимок четырёх прогонов](docs/mixamo-models-performance-2026-09-15.json). Chrome 152, ANGLE Metal, Apple M1 / 16 GiB; измеренный CSS-размер 1164×655, DPR 1,10 (примерно 1280×720 физических пикселей), фиксированный ракурс, 5 секунд прогрева + 12 секунд измерения. Временные герои `npm run stress`, обычные сохранения и production не используются. Остальные пользовательские приложения не закрывались.

| Сценарий | FPS | p95 кадра, мс | Кадров >50 мс | Код кадра, мс | Draw calls | Треугольники с проходами | GPU query, мс | p95 серверного тика, мс |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 герой | 57,0 | 17,7 | 5 | 7,5 | 170 | 1 103 052 | 10,7 | 1,02 |
| 16 героев трёх классов | 28,1 | 49,0 | 8 | 32,1 | 420 | 1 702 302 | 22,1 | 7,28 |
| 16 без теней | 43,7 | 33,5 | 8 | 19,0 | 246 | 915 161 | 8,6 | 14,76 |
| 16 обычная графика, повтор | 41,8 | 33,6 | 5 | 18,8 | 420 | 1 702 302 | 7,7 | 13,18 |

Стабильные 60 FPS в толпе **не достигнуты**. В первом полном прогоне кадр до 2182 мс приходится в основном на render-submit; в повторе среднее время кадра ниже. Загрузка/компиляция/драйвер и фоновые задачи — возможные причины, этим замером они не разделены. Отключение теней сокращает геометрию/вызовы, но 43,7 против 41,8 FPS после прогрева не доказывает, что тени — единственная или основная проблема. Старые и новые бинарные модели в одинаковых условиях не сравнивались; исторические 60 FPS предыдущего раздела нельзя считать доказательством регрессии только из-за Exo.

GLB воина 5,94 МБ (60 356 треугольников всех альтернатив), лучника 3,22 МБ (20 526 треугольников тела), мага 1,93 МБ (14 235). Видимые части — подмножество ассета; счётчики таблицы включают окружение и тени. Каждая модель загружается по необходимости и кэшируется; геометрия/текстуры между героями общие, скелет каждого героя независим. Сжатие непрозрачных текстур в JPEG 88 сократило первоначальный PNG-экспорт воина с 17,5 до 5,94 МБ; это размер загрузки, не доказанное увеличение FPS.

Следующий этап производительности — контролируемое сравнение после прогрева и бюджет рендера толпы: LOD для дальних героев, обновление удалённых анимаций по частоте/расстоянию, стоимость материалов и теней. Массовый бой, длительные сессии и слабые устройства этим коротким проходом не проверены.


## Четыре новые коллекции, шесть навыков и 19 мобов — 15 сентября

[Снимок семи прогонов](docs/class-content-performance-2026-09-15.json). Изолированный стенд 4742, временные герои, Chrome/M1, фактически записанный CSS-размер **1527×759, DPR 1,10**, обычные тени и зум 1. Запрошенный viewport override не дал 1280×720, поэтому ниже используются измеренные размеры. 5 с прогрева + 12 с записи; вкладка не теряла фокус. Фоновые пользовательские приложения оставались открыты. Наблюдение процессов после замера показало заметную стороннюю CPU-нагрузку; её влияние не изолировано.

| Состояние / сценарий | FPS | p95 кадра, мс | >50 мс | Код кадра, мс | Draw calls | p95 серверного тика, мс |
|---|---:|---:|---:|---:|---:|---:|
| До отсечения, 1 в лагере | 42,5 | 34,7 | 2 | 10,7 | 298 | 1,9 |
| До отсечения, 16 AFK | 28,1 | 51,4 | 25 | 29,6 | 594 | 22,6 |
| До отсечения, 16 в лагере | 33,0 | 35,4 | 5 | 26,2 | 706 | 11,9 |
| После интеграции, 1 в лагере | 45,6 | 34,1 | 0 | 8,9 | 199 | 3,3 |
| После интеграции, 16 AFK | 40,3 | 34,9 | 1 | 16,9 | 377 | 13,1 |
| После интеграции, 16 в лагере | 13,6 | 151,0 | 94 | 63,1 | 646 | 28,4 |
| После интеграции, 16 в лагере, повтор | 12,0 | 150,0 | 94 | 72,9 | 645 | 34,2 |

Включено отсечение мобов по проверенным границам всех клипов; дальние группы не рисуются и не сэмплируют Mixer за пределами камеры с запасом 5 м. В 16-AFK прогоне средний GPU query уменьшился с 57,6 до 31,4 мс, число вызовов рисования — с 594 до 377. Одновременно подключалась публикация реальных коллекций лучника/мага другим игрокам; динамические бои не являются побитово одинаковой сценой. Поэтому всё изменение FPS нельзя приписать одному отсечению.

**Плотная группа остаётся проблемой.** Два действительных прогона дали лишь 12–14 FPS и по 94 кадра дольше 50 мс. Повтор не снял проблему. У первого медленного прогона среднее render-submit 44,9 мс, actors 16,8 мс, GPU query 25,5 мс, 91 long task (максимум 1179 мс). Это требует отдельного профиля толпы и отделения сторонней нагрузки; успешный AFK-прогон и меньшее число draw calls не подтверждают стабильные 60 FPS. В обычном одиночном наблюдении после прогрева индикатор поднимался до 53–57 FPS, но это не заменяет таблицу.

В обоих 16-AFK прогонах к концу оставались активными 16/16 героев; за окно измерения принято 22 навыка и 24 атаки, пик восемь снарядов, ошибок соединения нет. Часть времени герои ждали возрождений; это два коротких спота, а не непрерывное массовое PvP. AFK использовал уровень 10, реальные HP/MP/зелья и средние броски шести вещей класса. Все шесть ID навыков дополнительно проверены тестами; короткий AFK-сценарий сам по себе не гарантирует использование каждого ID. Длительный бой, экономика за часы и производительность Hetzner/слабых компьютеров этой проверкой не подтверждены.

## Открытая карта, PostgreSQL и новый интерфейс — 15 сентября

[Числовой снимок двух финальных прогонов](docs/open-world-performance-2026-09-15.json), код `62c855c`. Временный PostgreSQL-стенд 4742, карта 110 × 90 м, 41 моб; Chrome 152 / ANGLE Metal / M1 / 16 GiB. Фактическое окно 1527 × 810 CSS px, DPR 1,10, зум 1, тени включены. Каждый прогон: 5 секунд прогрева и 12 секунд записи, без потери фокуса. Полный набор тестов в это время не выполнялся; пользовательские приложения оставались открыты.

| Сценарий | FPS | p95 кадра, мс | >50 мс | Код кадра, мс | Draw calls | Треугольники с проходами | GPU query, мс | p95 серверного тика, мс |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 16 AFK, три класса | 36,5 | 34,8 | 4 | 14,3 | 324 | 968 319 | 34,0 | 11,76 |
| 1 герой в лагере | 42,5 | 34,3 | 2 | 9,8 | 253 | 789 545 | 29,6 | 2,62 |

В AFK сохранились 16/16 активных героев: 16 навыков, 16 атак, пик семь снарядов, ошибок соединения нет. Мобы умирали и ожидали возрождения, поэтому число живых существ менялось; это не непрерывная максимальная боевая нагрузка. В лагере живы все 41 моб. Максимальный интервал кадра — 51,8/51,9 мс; 0/1 long task соответственно. Браузерная консоль без ошибок.

**Стабильные 60 FPS пока не достигнуты.** В AFK среднее время actors составляет 3,3 мс, render-submit — 10,3 мс; в лагере — 2,1 и 7,2 мс. GPU query указывает на существенную стоимость вывода сцены, но вместе с render-submit эти значения не складываются и не отделяют конкуренцию других приложений за GPU. Следующая отдельная проверка — одинаковая сцена с изменением DPR/теней, затем бюджет материалов, геометрии и анимаций. Эти измерения не обосновывают перенос игровой логики на Rust.

Средний серверный тик 16 AFK — 9,1 мс, из них broadcast 8,6 мс; максимум тика 84,4 мс, p99 интервала 102 мс при цели 50 мс. За 12,1 с отправлено около 81,8 МБ суммарно всем 16 соединениям. Перед увеличением онлайна нужны сокращение повторных полных снимков и отдельная проверка сетевой нагрузки. Сохранения PostgreSQL работали в этом же процессе, но продолжительный AFK и авария всего хоста здесь не моделировались.

При подготовке стенда исправлены повторное подключение ботов во время записи БД, повторная выдача старых ID вещей и очередь команд предыдущего сценария. Последняя выключала только что включённую AFK-охоту. Прежний отчёт `1789485993113-fy7v5.json` имел лишь 2 активных AFK из 16 и исключён из вывода о нагрузке, хотя проверка фокуса/таймингов считала его действительным. Новый сетевой тест проверяет два перехода лагерь → 16 AFK с сохранением 16 активных героев. Предыдущие одиночные замеры 26–27 FPS выполнялись при другой фоновой активности браузеров и не являются контролируемым сравнением «до/после». Исходные отчёты сохранены в игнорируемом `artifacts/performance/`.

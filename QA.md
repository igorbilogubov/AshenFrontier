# Проверка общей 3D-опушки

14 сентября 2026. Основная проверка — браузер, сервер и сохранения. По отдельному запросу добавлена проба Godot Web/macOS; прежние записи без нативных сборок относятся к основному клиенту.

Текущий набор — 52 теста. Последний проверенный игровой релиз — `20260914-3d-03`; более ранние результаты ниже отмечены как история. Общая точка продолжения: [STATUS.md](STATUS.md), порядок проверки веток — [WORKFLOW.md](WORKFLOW.md).

## Проба Godot — 14 сентября 2026

Отдельный клиент в `godot-prototype/`, Godot 4.7.2.stable.official, Compatibility. Использует существующие GLB/протокол 2, отдельный Node backend 4733, Web 4741, данные `data/godot-prototype/`. Основная игра 4732 не заменялась. Инструкции: [README](godot-prototype/README.md).

- `npm run check`: 23 JavaScript-модуля. `npm test`: 52/52.
- Импорт пяти канонических ассетов и release-экспорт Web/macOS выполнены. Исходные Blender/GLB и серверная симуляция не менялись.
- `npm run godot:test`: реальный временный сервер, отдельная сессия Godot, семь мобов, семь клипов героя, изменение реальной позы стопы во время ходьбы. Проверены удержание/отпускание ЛКМ, отсутствие движения/поворота от наведения и WASD/стрелок, остановка над HUD и при потере фокуса, Shift, фиксированный ракурс и колесо.
- Тест проходит путь к волку, удерживает атаку до серверной награды, затем проверяет переподключение и полное пересоздание клиента с чтением записанного ключа. Сохранённые XP/золото и идентичность совпадают. Headless-результат не является измерением графики.
- Регрессия материалов: вершинная окраска `Layered_Fur` включена у всех животных. Отдельно проверяется триангуляция жидкости колб на 10 001 значении от 0 до 1; при почти нулевом HP устранён вырожденный полигон.
- Web просмотрен в живом браузере: читаемые панели, красная HP слева, синяя MP справа, персонаж и окрашенные звери, движение удержанием, бег, приближение, анимация удара, телеграф и потеря здоровья. Убийство волка выдало **12 XP и 8 золота**. После смерти герой вернулся в лагерь. После штатного рестарта тестового сервера и reload клиента эти награды остались.
- Отдельное macOS-приложение запущено и просмотрено. Web и macOS одновременно показывали **двух игроков**; это проверка совместимого входа и общего мира, не испытание вместимости.
- Web-наблюдения менялись примерно от **17 до 60 FPS**. Один замер после прогрева дал p95 переданного в `_process` delta **16.67 мс**, viewport Godot 960×937, два игрока и семь мобов. В ранней нативной сессии при параллельной работе клиентов/захвате окна p95 этого delta достигал **144.44 мс**. Это показания игрового цикла, не GPU-профилирование или независимое измерение времени кадров. Причина разброса не изолирована; нативная производительность не подтверждена стабильным замером.

Окружение, освещение, шейдеры, разрешение и обработка анимаций отличаются от Three.js. Эти результаты **не доказывают превосходство одного движка**. Предсказание под задержку сети, все три класса в интерфейсе, статы/инвентарь, чат, мобильные платформы и нагрузка не проверены в Godot. Для решения о миграции нужен отдельный сопоставимый прогон. Push и deployment не выполнялись.

## Автоматические проверки

На Node 24: `npm run check` проверяет синтаксис 19 JavaScript-модулей; `npm test` — 52 успешных теста.

- Миграция трёх классов: личность, уровень, деньги и вещи сохраняются, позиция заменяется лагерем новой карты.
- Версия 3 сохраняет здоровье, ману, распределённые очки, ревизию, координаты, откаты, смерть и переполненные награды. Миграция версии 2 сохраняет 3D-прогресс и выдаёт соответствующий уровню бюджет однократно.
- Сервер ограничивает скорость, столкновения, дальность, частоту атак и поддельные пакеты; просроченный ввод останавливается.
- Босс общий, два участника получают личный прогресс/добычу; содержимое чужих рюкзаков и секреты не раскрываются.
- Квест не выдаёт награду повторно; шесть слотов, продажа и полная сумка сохраняют целостность вещей; попытки смены класса не меняют героя, статы или снаряжение.
- Стрелы и магия имеют серверные траектории/столкновения; мобы предупреждают об атаке и возвращаются домой.
- Реальные HTTP/WebSocket-клиенты: движение видно другому игроку, дублирующий/неверный ключ отклоняется, совместный бой, переподключение и штатный рестарт сохраняют данные.
- Закрытые файлы и старый клиент возвращают 404; GLB имеет правильный Content-Type, чужой Origin отклоняется.
- GLB героя/животных: структура скелетов и клипов, конечные координаты, независимые экземпляры, циклы походки и фазы атаки.
- Отдельная регрессия: числовая сила удара не подменяет объект атаки и не ломает предсказание позиции героя.

## Неизменяемый класс героя — 14 сентября

Класс выбирается только при создании. В локальной ветке `codex/character-stats` удалены выбор класса у костра и выдача нового оружия/возврат очков при его смене. Прежняя команда `class` отклоняется сервером. Исторические сценарии смены класса ниже относятся к предыдущей версии.

На Node 24 проходят **52/52** теста и проверка синтаксиса **19 модулей**. Обновлённые тесты проверяют запрет для всех трёх классов в лагере, вне лагеря, при смерти/атаке/бое и полной сумке, сохранность всех постоянных полей героя, классового оружия и вложенных очков. Реальные WebSocket-сессии подтверждают отклонение команды и приоритет сохранённого класса над `classId` при входе по ключу, переподключении и после штатного рестарта, включая героя версии 2.

Локальный сервер 4732 штатно перезапущен. Сравнение сохранённого прогресса до/после подтвердило неизменность классов, уровней, XP, золота, вещей, экипировки, статов и квестов всех трёх локальных героев. В браузере у существующего мага раскрыта «Подготовка у костра»: доступен сброс статов, выбора класса нет, прогресс восстановлен. Ошибок/предупреждений в журнале вкладки нет; `/health` отвечает успешно с версией сохранений 3. Push/deployment не выполнялись.

## Колбы HP и MP в нижнем меню — 14 сентября

Локальная ветка `codex/character-stats`, preview на порту 4732. `npm run check`: **19 модулей**; `npm test`: **52/52**; `git diff --check` без замечаний. Новые тесты игровой логики не добавлялись: изменение относится к представлению текущего состояния.

В браузере проверены маг на обычном размере 740×723 и отдельный QA-воин с кнопками обоих видов оружия, широкая раскладка 1280×720 и узкая 390×844. Красная колба расположена слева, синяя справа от кнопок, круги не деформированы; горизонтального переполнения нет. Открытие/закрытие характеристик сохраняет доступность нижних кнопок, колбы скрываются на время окна. После вложения пяти очков в энергию тестового воина максимум маны вырос с 40 до 60 без мгновенного заполнения: в колбе видны свободное место сверху, текущие 47/60 и последующая регенерация (следующий DOM-снимок — 49/60, заполнение 82,68%). Полный запас здоровья/маны также проверен. Уровень жидкости использует дробное значение, число запаса округляется как прежде.

В журнале QA-вкладки нет ошибок/предупреждений. Настройка размера сброшена, QA-вкладка закрыта, исходная игра пользователя обновлена. Серверные правила и сохранения не менялись, push/deployment не выполнялись.

## Управление удержанием ЛКМ — 14 сентября

Проверено локально на `http://127.0.0.1:4732/` в ветке `codex/character-stats`. Синтаксис **19 модулей**, полный набор **52/52** и `git diff --check` проходят. Пять новых тестов проверяют текущую сторону курсора, одинаковую скорость по направлениям, отсутствие движения/поворота при наведении без удержания, торможение после отпускания, зону остановки у ног, невалидную точку/смерть, дистанцию атаки и прекращение преследования.

В браузере выполнены перетаскивание с зажатой ЛКМ по земле, смена стороны курсора, отпускание над сценой и над кнопкой интерфейса, нажатия WASD/всех стрелок, клик по миникарте, открытие/закрытие окна героя. Отдельный WebSocket-наблюдатель подтвердил движение браузерного героя во время жеста и нулевую скорость после отпускания; после WASD/стрелок и миникарты координаты и угол не изменились. В заключительных 98 снимках за 5 секунд координаты, угол и скорость оставались постоянными. В журнале вкладки нет ошибок/предупреждений. Наблюдатель отключён, игра оставлена открытой.

Свободное наведение без кнопки проверено автоматическим тестом логики ввода; отдельного действия hover у используемого браузерного инструмента нет. Длительное удержание в бою и отпускание вне окна браузера в этой ручной проверке не воспроизводились. Захват указателя, потеря фокуса/соединения и открытие интерфейса сбрасывают удержание в коде. Подсказки обновлены, точечный маршрут удалён. Push и deployment не выполнялись.

## История: ручные характеристики и новый инвентарь — 14 сентября

Локальная ветка `codex/character-stats`, отдельный сервер `http://127.0.0.1:4732/`, собственный каталог `data/`. Push и deployment для этой работы не выполнялись. После изменений полный набор **47/47** проходит на Node 24, синтаксис 18 модулей и `git diff --check` проверены.

Добавлены проверки четырёх характеристик/трёх классов, начального и уровневого бюджета, реального повышения уровня после убийства, повторных/устаревших/поддельных пакетов, атомарности, ограничений лагеря и боя, сброса/смены класса без лечения, переполненной сумки, миграции версии 2, восстановления версии 3 без повторной выдачи маны, попаданий/промахов ближних и дальних атак, расхода маны, регенерации и уменьшения входящего урона. Отдельный тест с настоящими WebSocket-соединениями проверяет старого героя, распределение, приватность, переподключение и штатный перезапуск.

В браузере проверены:

- Воин: черновик +1 силы/+1 живучести показывает урон 26 → 26,8 и HP 100 → 104; применение тратит два очка, сервер подтверждает, обновление страницы сохраняет результат. Бесплатный сброс возвращает бюджет.
- Лучник: +1 ловкости показывает одновременное изменение урона, точности и защиты; отмена убирает черновик. Маг: +1 энергии повышает урон, максимум маны на 5 и её регенерацию на 0,075/с. Смена класса возвращает вложенные очки.
- Фокус кнопки сохраняется при обновлениях мира. `Q` и `W` при открытом окне не вызывают боевую команду и движение. Кнопки действий учитывают соединение, лагерь и состояние героя.
- Компоновка при фактических CSS-размерах 1067×750 (две панели), 740×723 (вкладки) и 325×703 (компактный режим) проверена. На узком экране нет горизонтального переполнения; панель прокручивается, подтверждение и закрытие доступны. Настройка размера браузера после проверки сброшена.
- Маг в общей локации победил волка, получил 12 XP, 8 золота и посох. Смерть/возрождение и обновление страницы сохранили награды и распределение. Новый инвентарь позволил выбрать «Посох сумерек +5», надеть его у костра; надетую вещь нельзя продать. Видны ровно шесть слотов и 16 ячеек сумки.
- Исправлены перекрытие имени героя чатом после добавления маны, видимость пустых ячеек и точность чисел предпросмотра. В журнале просмотренной вкладки нет ошибок и предупреждений.

Таблица формул для одинакового бюджета на уровнях 1/10/20/60 выявила и помогла убрать чрезмерный рост защиты лучника. Сводка и ограничения расчёта — [PROGRESSION.md](PROGRESSION.md). PvP, AFK и длительное испытание баланса ещё не проводились; рост FPS этой работой не заявляется. Модели и анимации не менялись.

## История: браузерная проверка перехода на общий 3D-мир

Два независимых профиля в общем мире: воин «Дозорный» и маг «Искра». В обоих видны второй персонаж и счётчик двух игроков; перемещение синхронизировано. Маг вышел из лагеря, убил волка, получил 12 опыта, 8 золота, 1/5 задания и «Посох сумерек +5»; надел его у костра. Обновление страницы и остановка/запуск сервера восстановили того же мага с теми же наградами и надетым посохом. Соединения восстановились автоматически. Смерть и возвращение в лагерь не обнулили прогресс.

Проверены широкий и узкий браузерные размеры, интерфейс шести слотов и прокручиваемого рюкзака. После устранения ошибки состояния атаки герой виден, новые ошибки рендера не появляются. Около 59–60 FPS в проверенной сцене с двумя героями — наблюдение, не нагрузочная гарантия.

## Публичная проверка и её история

Для проверки HTTPS/WSS используется `node scripts/smoke-online.mjs <адрес>`: два новых QA-героя, общая видимость, движение и серверная запись. Ключи сохраняются только в закрытом `artifacts/shared-3d/online-heroes.json`. После штатного рестарта только игрового сервиса:

```sh
node scripts/smoke-online.mjs https://ashen-frontier.91.99.21.123.sslip.io --restore
```

Сравниваются id, класс, уровень, опыт, золото, вещи, экипировка, координаты и задача. Эта команда проверяет сохранения текущего протокола, не старый пиксельный мир. При первом 3D-релизе публичные HTTPS/WSS и восстановление двух героев после рестарта проверены успешно. Браузер загрузил 3D-опушку и переподключился; сохранились все 11 исходных героев. К последнему релизу `20260914-3d-03` записей было 15, их основные поля прогресса также сохранились. Девять других контейнеров не менялись, шесть прежних HTTPS-адресов отвечали 200. Подробности в [DEPLOYMENT.md](DEPLOYMENT.md).

В новом clone файла с QA-ключами нет. `--restore` требует ранее созданного закрытого `artifacts/shared-3d/online-heroes.json` для того же адреса. Обычные `npm test` не требуют ключей и не подключаются к production. Отчёты из `artifacts/` остаются локальными; проверяемые сценарии и результаты описаны здесь.

## Не покрыто текущей проверкой

Длительная нагрузка с 16 игроками, слабые устройства, аварийная потеря VPS, AFK-начисления, PvP/PK, длительный баланс ручных сборок, рынок и платежи. Мобы и чат перезапускаются вместе с миром. Сохранения JSON с двухсекундным интервалом ещё не заменяют транзакционную экономику. Аккаунты с восстановлением доступа и отдельные модели/клипы дальних классов предстоит сделать.

## История: исправление свободной прогулки — 14 сентября, 3d-02

Причина рывков была общей для старых процедурных и новых GLB-мобов: патруль следовал за движущейся точкой с порогом остановки 0,12 м. В двухминутной симуляции без игроков получалось 821–876 переходов движение/остановка в минуту на моба, с типичным рывком около 0,065 с.

После замены на постоянную цель, разгон/торможение, поворот и полноценную паузу: 15,5–21 переход в минуту, непрерывная ходьба в среднем 3,9–5,7 с. Отрезков ходьбы короче 0,2 с нет. Все семь мобов остаются в разрешённой области вне лагеря; агрессия прерывает патруль немедленно, скорость погони сохранена, смерть сбрасывает маршрут. Два регрессионных теста добавлены, полный набор 29/29 проходит. В браузере проверены прогулка у лагеря и отсутствие ошибок рендера. Геометрия и клипы в этом исправлении не менялись.

## Доработка волка — 14 сентября

Обычный волк пересобран в Blender, кабан и вожак совпадают с прежними файлами по SHA-256. У волка 24 кости, 18 154 треугольника, восемь клипов. Все 31 тест проходят: дополнительно проверены опора четырёх лап в шаге, рыси и двух поворотах, замыкание циклов и непрерывность смешанной позы при 30/60 FPS. Проверки атаки, смерти, возрождения, сети и сохранений также проходят.

В бестиарии проверены переключение «до / после» в одинаковой фазе шага и воспроизведение поворота. В локации проверены загрузка нового волка и патрулирование, ошибок рендера нет. В этой сессии браузер показывал около 23–28 FPS в локации; это отдельная проблема производительности, улучшение FPS этой правкой не заявляется. Отчёты и журнал сборки: `artifacts/wolf-polish/`. Подробности модели и ограничения: `art/creatures/ashen-fauna-v1/README.md`.

## Проверка репозитория и параллельной работы

GitHub Actions (`.github/workflows/ci.yml`) запускает `npm ci`, `npm run check`, `npm test` на Node 24 для PR и push в `main`. Workflow не использует production-данные и не выполняет deployment. Для локальной проверки после clone достаточно этих же команд: нужные GLB и отчёт сборки воина входят в Git.

Для ручного браузерного теста ветки используются свой порт, `GAME_PREVIEW_ALIAS=0` и её собственный `data/`. Проверка в разных вкладках одного сервера использует разные QA-сессии; разные порты/worktree являются разными мирами. Перед PR обновляются результаты именно своей задачи; исторические 27/29 тестов и прежние замеры FPS не подменяют новый результат.

## TypeScript — 15 сентября 2026

Основной игровой код проверяется с `strict`, `noEmitOnError`, `verbatimModuleSyntax`. Клиент, сервер, общие формулы и контракты компилируются в `dist/`; исходники не выдаются HTTP. Все прежние 52 теста проходят на собранных модулях, включая настоящие WebSocket-сессии, миграции V2/V3, неизменяемый класс и сохранение после перезапуска. Перенос языка сам по себе не является доказательством ускорения.

# Проверка общей 3D-опушки

## 17 сентября 2026: AFK — один атакующий навык и бафы по откату

Локально, без push. `npx tsc --noEmit` и `node scripts/check.mjs` (93 модуля) прошли. `test/afk-preferences.test.mjs`, `test/afk.test.mjs`, `test/skill-builds.test.mjs`, `test/client-imports.test.mjs`: 52 пройдено, 2 PG-сценария пропущены без локального PostgreSQL. Старый `skillOrder` принимается только как миграция сохранения.

## 17 сентября 2026: покупка ячеек рюкзака и сундука

Ветка `codex/boss-loot-tables` поверх лута босса. Ячейки уменьшены, сетка 6 колонок, плюс докупает слоты.

- `npx tsc --noEmit` успешен.
- `node scripts/check.mjs` — 93 модуля, Docker context OK.
- Без локального PostgreSQL (`pg_ctl` недоступен): `test/inventory-slots.test.mjs` и связанные `inventory`/`personal-stash`/`vendor`/`ground-loot`/`loot-rarity-v2`/`dungeons`/`equipment-100`/`client-imports`/`shared-world`/`consumable-inventory` — 69 пройдено, 0 падений; PG-сценарии schema 9 пропущены.
- Новый герой больше не падает на `Invalid consumable overflow`: `bagCapacity` задаётся до расчёта overflow.
- Push не выполнялся.



## 17 сентября 2026: опции редкости и лут босса

Ветка `codex/boss-loot-tables`. Прогнаны изолированные сценарии без PostgreSQL:

- `test/loot-rarity-v2.test.mjs` — белые чаще зелёных, элита без жёлтого/сета, босс без белых, 1/2/3/4 опции, 1–3 вещи босса.
- `test/equipment-100.test.mjs` — 10 000 бросков босса: 50/30/15/5, без пустых и белых.
- `test/target-presentation.test.mjs`, `test/snow-environment.test.mjs` — подсказки цели.

17/17 прошли. Полный `npm test` с временной PostgreSQL на этом срезе не запускался: текущий `main` уже не собирает `newHero` из‑за `bagCapacity`/`BAG_CAPACITY` (ошибка была до этой правки). Push не выполнялся.

## 16 сентября 2026: движение кликом и удержанием, массовые навыки без кулдауна

Финальное уточнение управления после `b205ab3`. Node.js 24, отдельная временная PostgreSQL; обычные данные и production не использовались.

- `npm run check` и строгая сборка прошли. Полный стандартный `npm test`: 358/359, стресс-сценарий не дождался ожидаемого числа игроков при переходе 16 → 1. Затем весь тот же набор на новой временной PostgreSQL с `--test-concurrency=1`: **359/359**, без пропусков, 151,1 с. Игровой код и тайм-ауты между прогонами не менялись. Логи — `artifacts/mixed-input/full-test.log` и `sequential-test.log`; отдельный последовательный runner повторяет штатный harness с одним рабочим процессом.
- Три новых сценария `mouse-walk.test.mjs` проверяют маршрут короткого клика после отпускания, отсутствие поворота от наведения, переход удержания на текущий курсор, смену направления, остановку без возобновления маршрута, мёртвого героя и некорректную позицию.
- Серверные регрессии трёх AoE проверяют нулевой кулдаун, расход маны, запрет одновременной атаки и повтор сразу после завершения анимации. Старые положительные откаты этих трёх навыков обнуляются при загрузке, отсутствующий ключ не добавляется. Последнее уточнение исправило несовпадение точного round-trip сохранённой сборки, обнаруженное агентским полным прогоном.
- Chromium/Playwright на изолированном игровом процессе: короткий клик достиг `{x:273.99,z:10.45}` с ошибкой меньше 0,2 м после отпускания; перемещение курсора без нажатия не переназначило путь. Удержание переключилось с направления примерно `{x:.99,z:-.11}` на `{x:-.07,z:-1}` вслед за курсором. После отпускания позиция осталась `{x:274.15,z:9.28}`, серверный маршрут отсутствовал. Shift+ЛКМ дал два удара без перемещения.
- Удержание ПКМ выполнило три «Землетрясения» за 2,224 с серверного времени, кулдаун оставался 0; мана 238 → 156,36 с учётом регенерации. Снимок осмотрен: `artifacts/mixed-input/repeated-area.png`. Для лучника и мага повтор проверен серверными сценариями.
- Перенос удерживаемой мыши на настоящую кнопку HUD и отпускание над ней остановили ввод: позиция осталась `{x:269.21,z:10.96}`, маршрут отсутствовал. Первый пробный замер над прозрачной полосой XP ошибочно сравнивал состояние до обработки отпускания; финальный сценарий ждёт подтверждённую остановку и проверяет последующую неподвижность.

Независимая проверка логики pointer capture/release, потери фокуса, Shift и серверного ввода не выявила оставшихся дефектов. Игровых ошибок JavaScript нет; единственное сообщение консоли — прежний 404 `/favicon.ico`. Временный игровой сервер/кластер и браузер после проверки остановлены. Полный FPS-стресс и длительный баланс массовых навыков этим не проверялись. Слияние и push в `main` разрешены пользователем; deployment не запрашивался.


## 16 сентября 2026: сеть порталов, управление, пятый навык и редкость добычи

Локальная ветка `codex/portals-controls`, Node.js 24.21.0, protocol 3/schema 8. Все сетевые и SQL-проверки выполнены на временной PostgreSQL; обычный мир, рабочие данные героев и production не использовались.

- Итог полного набора без браузерной нагрузки: **354/354**, без пропусков, 50,6 с. Лог — `artifacts/portals/final-quiet-test.log`. `npm run check`, строгая сборка и `git diff --check` прошли.
- Первоначальный прогон: 348/352. Исправлены устаревшие ожидания размера каталога/белого пула/элитного шанса, а в новом AoE-тесте расход маны проверяется до последующего естественного регена. Отдельный повтор затронутых файлов: 30/30.
- Промежуточный повтор под браузерной нагрузкой: 352/354, сбои ожидания стресс-сценария и проверки маны по позднему снимку. Проверка маны теперь использует первый снимок атаки и его серверный возраст для точного учёта регенерации. Отдельный повтор: 2/2; тайм-ауты сервера не менялись.
- Серверные сценарии проверяют все 15 безопасных проходимых точек, ограничения уровня/золота/боя/расстояния, однократное списание, сохранение цены и позиции после рестарта, маршрут вокруг ограды, продолжение после нейтральных пакетов и прекращение после отключения. Сборки меняются в бою без лечения, сброса откатов и ускорения серии ударов.
- Миграция 7 → 8 сохраняет четыре прежних назначения, таланты и пресеты, добавляя пустой пятый слот. Проверены новые области всех классов, предел целей/радиус/мана/откат, белые пулы и неизменность сохранённых вещей.
- В браузере обнаружен отказ навыка вокруг героя при далёком противнике под курсором. Earthquake/nova теперь игнорируют такую цель; отдельная регрессия подтверждает отсутствие движения и попадания за радиусом. После исправления файл навыков: 21/21. Независимая проверка интеграции новых команд/панелей не выявила оставшихся дефектов.

Настоящий Chromium через Playwright, отдельный игровой процесс и fixture-cookie:

- Клик по городскому порталу открыл список назначений. Переход в снег списал ровно 80 золота (100000 → 99920), доставил в `{x:269,z:11}` и закрыл меню. Портал и безопасная окружность видны в 3D; отдельный значок есть на миникарте.
- ЛКМ по земле после отпускания довёл героя к точке `{x:273.99,z:10.45}` с ошибкой меньше 0,2 м. Удержание Shift+ЛКМ дало два удара без изменения позиции и режима бега. Клик по снежной рыси отправил `approach:true`, и обычные атаки продолжились после отпускания.
- ПКМ запустил пятый навык «Землетрясение»: сервер списал 28 MP и показал откат 13 секунд. В книге пять слотов; через интерфейс на ПКМ перенесён «Вихрь» и добавлен один ранг таланта. Применение во время активного удара подняло `buildRevision` до 1 и сохранило выбранную сборку. Герой находился под настоящими атаками рыси.
- Удержание пробела отправило только один `pickupNearest`. После убийства рыси отдельный пробел подобрал одну личную стопку: золото 99920 → 99948, объектов на земле 1 → 0.
- Панель порталов и книга талантов проверены по реальным DOM-границам на 1440×1000, 1146×950, 1280×720, 900×600 и 390×844: остаются выше HUD, кнопка применения находится внутри книги. Осмотрены снимки `travel-1440.png`, `travel-390.png`, `book-1146.png`, `earthquake.png`; остальные свидетельства — `artifacts/portals/`.
- Игровых ошибок JavaScript и предупреждений не зарегистрировано; единственная ошибка консоли — прежний 404 `/favicon.ico`. Браузер и изолированный сервер/кластер после проверки остановлены.

Артефакты исключены из Git. Все 15 переходов и три новых AoE покрыты серверными тестами, но ручной 3D-проход каждого назначения и визуальная проверка каждого класса не выполнялись. Нагрузочный FPS-прогон и длительное наблюдение экономики не проводились; цены и проценты — начальная настройка баланса. Push и deployment этого пакета не выполнялись.

## 16 сентября 2026: бой, перемещение, миникарта, онлайн и границы окон

Проверки текущей копии `/Volumes/StableOS 1/Projects/AshenFrontier-main`, Node.js 24.21.0, protocol 3/schema 7. Репозиторий был предоставлен без `.git`; создана локальная исходная фиксация `2040f87`, задачи выполнялись в отдельных worktree. Это локальная проверка, не deployment и не реальный Google OAuth. Рабочие данные героев и production не использовались.

- `npm run check` и строгая сборка прошли. `git diff --check` чистый.
- Полный `npm test`: 337 тестов, 335 прошли, два сетевых сценария не уложились в ожидание запуска/набора игроков при параллельной нагрузке. Повтор только `gameplay-travel-network.test.mjs` и `stress-network.test.mjs` на отдельной временной PostgreSQL: **2/2**, без изменений реализации или увеличения тайм-аутов. Чистый единый прогон всех тестов этим не заявляется.
- После добавления списка онлайн и окончательной интеграции: `online-roster.test.mjs`, `consumable-status.test.mjs`, `minimap.test.mjs` — **9/9**. Проверены глобальный список, исключение отключённых героев, минимальный состав публичных полей, смена локации, назначения Q/W и общий откат ресурса, фильтрация маркеров/проекция карты.
- Серверные регрессии охватывают смену экипировки/распределение в активной атаке без лечения и сброса откатов, все семь конечных выходов, пятую секунду возврата, повторную отмену, движение/атаку/урон/отключение и агрессию именно на завершающем тике. WebSocket/PostgreSQL подтверждают отсутствие сохранения незавершённого возврата и сохранение конечной позиции.

Настоящий Chromium через Playwright, изолированный игровой сервер и fixture-cookie в временной PostgreSQL:

- Под атаками волков через интерфейс применена Сила (`statRevision=1`) и надета другая броня (`qa-armor`); сервер продолжал бой. Зелье показало «4 с», затем `quick-ready` и однократную `quick-refreshed`-анимацию. Снимки: `artifacts/usability/cooldown.png`, `combat-equipment.png`.
- Кнопка возврата показала отсчёт, повторный клик и настоящее движение мышью отменили его. Новый отсчёт завершился не раньше 4,5 с после получения первого снимка и вернул в `{x:.5,z:4}`. Клик «Добыча» и Z меняют состояние; выбранное скрытие сохранилось после reload. `town-countdown.png`.
- Книга талантов не пересекает HUD при 1440×1000, 1146×950, 1280×720, 900×600 и 390×844; проверены реальные DOM-границы. Инвентарь также заканчивается выше HUD. `panels-1146.png`, `panels-390.png`. Полоса пресетов/применения остаётся видимой, содержание прокручивается.
- В «Онлайн» одновременно показаны воин 50 уровня в лесу и маг 25 уровня в Снежном пределе. После закрытия второго настоящего WebSocket его строка исчезла; после потери связи клиента показано ожидание, затем восстановлен актуальный список. Панель не перекрывает HUD на desktop/mobile. `online-desktop.png`, `online-mobile.png`.
- Миникарта визуально проверена в лесу; все регионы и новые выходы дополнительно покрыты общими данными и серверными тестами. Ручной проход каждого данжа в 3D не выполнялся. FPS-бенчмарк не запускался; наблюдавшиеся FPS не считаются сравнением производительности.

Артефакты и fixture-данные находятся в игнорируемом `artifacts/usability/` и не входят в коммиты. Консоль новой страницы не показала ошибок игрового JavaScript; был прежний 404 `/favicon.ico` и ожидаемые сетевые сообщения при проверке отключения.


## Опубликовано: выход из боя без задержки — 16 сентября 2026

Релиз `20260916-combat-exit-01`, runtime `a62c50f`, PostgreSQL schema 7. Бой завершается, когда нет живых мобов текущей локации, атакующих/преследующих героя, и собственной активной атаки/канала. Таймер 15 секунд больше не задерживает переходы после окончания столкновения; портал пересчитывает состояние сразу при обращении. Docker-сборка и запуск успешны. Тесты и ручная проверка по запросу пользователя пропущены. Перезапущена только игра; БД, Caddy и остальные контейнеры сохранены. Коммиты локальные, push не выполнялся.

## Зелья четырёх размеров и HP-реген в бою — 16 сентября 2026

Объединены `codex/potion-rules-v2`, `codex/potion-shop-ui` и migration 7. Строгая TypeScript-сборка успешна. На этапе правил прошли 24 игровых теста; пять PG-тестов в агентском запуске не выполнялись. После объединения точечный прогон на отдельной PostgreSQL: **17/17**, 0 пропусков, около 4,4 с — покупка, расход, назначение, WebSocket/повторный вход и миграция ограничений. Первый интеграционный прогон обнаружил hardcoded schema 6 в health; он исправлен на общую DATABASE_SCHEMA_VERSION, повторный затронутый набор полностью прошёл.

Проверены стопки всех восьми видов до 999 и лимиты SQL, сохранённые Q/W, запрет миграции при активном старом писателе. Обычная база героев для этого не используется. Полный набор, нагрузочный тест и ручная браузерная проверка не запускались; пользователь проверяет после deployment. Эти результаты не являются замером игрового баланса или длительности AFK. Runtime `d8cc6a6` опубликован как `20260916-potions-01`; healthy/schema 7 и неизменность Caddy/десяти других контейнеров подтверждены.

## Hotfix AFK / атака / FPS — 16 сентября 2026

Ветки `codex/afk-layout-hotfix`, `codex/afk-pickup-range` и `codex/attack-freeze` объединены; добавлены видимый FPS и общая подсказка радиуса 4 м. Агент AFK успел выполнить 318/318 тестов на изолированной PostgreSQL, агент интерфейса — 317/317 до объединения. Точечные проверки границ/анимаций модели — 8/8. Эти результаты не являются полным прогоном конечного объединённого релиза.

Диагностика цели с теми же Three.js/GLB: 20 попаданий курсора по кабану заняли 428,32 мс с полигональным raycast и 1,22 мс с границами; волк — 319,29 и 0,25 мс. Это измерение стоимости выбора цели, не замер FPS готовой игры. [Подробности](docs/ATTACK_TARGETING_PERFORMANCE.md).

По последнему указанию пользователя ручная проверка в браузере и повторный полный прогон пропущены; проверять игровой результат он будет сам после deployment. Новые production-герои, нагрузочный тест и резервные копии не создавались. Итоговая строгая TypeScript-сборка и Docker build успешны. Runtime `c24e3f4` опубликован как `20260916-afk-combat-01`; подтверждены healthy/schema 6 и неизменность Caddy/десяти соседних контейнеров. Ручного подтверждения устранения зависания/FPS этим нет.

## Production 20260916-world100-01 — 16 сентября 2026

Конечный runtime `b24dbb2`: `npm run check` успешен (82 модуля, 12 источников COPY); **317/317** тестов на Node 24 и отдельной PostgreSQL 18.6 внутри конечного Docker-образа, 0 пропусков, 205890 мс. [CI](https://github.com/igorbilogubov/AshenFrontier/actions/runs/35088989105) прошёл. Добавление двух публичных страниц проверено также пятью focused HTTP-тестами; браузерная проверка страниц и входа на 390×844 выполнена до выкладки.

Публичная проверка: 120 файлов совпали SHA-256, закрытые пути 404, health schema 6/writer true, account API configured true/maxCharacters 5. Настоящий Google-переход достиг экрана согласия; пользователь завершает его самостоятельно. Callback, создание героя, повторный вход и запрет двух окон не заявляются подтверждёнными реальным production OAuth-сеансом; их серверные контракты покрыты изолированными тестами. Production-герои для проверки не создавались. OAuth-приложение External / In production подтверждено в Google Cloud.

Caddy и 10 соседних контейнеров, включая БД, сохранили ID/start time, семь HTTPS-адресов отвечают 200. Резервное копирование и аудит сохранности героев пропущены по запросу пользователя; обычная база для нагрузочных тестов не использовалась. Артефакты в `artifacts/deploy-20260916-world100-01/`, детали в [DEPLOYMENT.md](DEPLOYMENT.md).

## Локально: контекст Docker для Google-входа — 16 сентября 2026

Удалённая сборка commit `668c45c` остановилась до создания образа: Dockerfile копировал `auth/`, но строгий allowlist `.dockerignore` не передавал этот каталог в build context. `auth/` добавлен в allowlist. Новый `scripts/check-docker-context.mjs` извлекает локальные источники `COPY` непосредственно из Dockerfile, проверяет их существование и требуемые правила `.dockerignore`; он запускается общим `npm run check` и не дублирует список упаковки вручную.

На Node 24.20.0 прошли `npm run check` (82 JavaScript-модуля, 12 локальных источников Docker `COPY`) и полный `npm test` — **317/317**, без пропусков, около 44 секунд. Локального Docker CLI нет, поэтому создание образа после исправления выполняется и проверяется отдельно на сервере перед переключением production. Push/deployment и обычные базы этим исправлением не затрагивались.

## Локально: семь регионов, подземелья и позднее снаряжение — 16 сентября 2026

Три параллельные ветки `codex/world-to-100`, `codex/progression-to-100` и `codex/dungeon-equipment` объединены координатором в `main`. Игровой предел — 100, 711 спавнов в семи полевых регионах, Стадиуме и семи подземельях. Добавлены 16 экспортированных моделей существ, 12 модульных комплектов и 72 иконки; каталог содержит 540 определений качества и 21 классовый сет.

Финальный `npm run check` — 81 модуль; `npm test` — **317/317**, без пропусков, около 107 с. Проверены настоящие PostgreSQL/WebSocket, сохранение новых качеств/сетов после повторного открытия хранилища, перенос жёлтой вещи в сундук, общие переходы и урон подземелий. Устаревший ожидаемый размер каталога обновлён; очистка тестовой БД больше не применяет FORCE к фоновому autovacuum чужой системной роли и всегда закрывает административное соединение. Дополнительных прав QA-роль не получила. Изоляция PostgreSQL та же, что в предыдущем этапе ниже: отдельная ограниченная QA-роль и случайные тестовые базы; новый системный кластер не запускается из-за ограничения shared memory. Общие герои и production для проверок не используются.

Браузерная проверка объединённого клиента:

- На отдельном `localhost:4841` герой вошёл в Склеп корней: верные название, миникарта, безопасный вход и серверный счётчик 12 стражей и оформленная панель задания. ЛКМ по выходу выполнила реальный серверный подход и переход обратно в лес; повторный вход во время преследования отклонён прежним правилом полевых порталов. Входные условия, переходы между всеми картами, блокировка босса, общая смерть стражей, три области урона и сброс подземелья проверяются серверными тестами.
- Настоящие экспортированные GLB и предупреждения босса просмотрены также в отдельной статической сцене. Сектор, круг и кольцо соответствуют общей геометрии. Это визуальный стенд, не доказательство прохождения боя по сети. Мелкая плитка пола и низкие стены не закрывают модель. Боссы используют увеличенные региональные модели; семи отдельных новых ригов нет.
- У воина 85 уровня снятие поздней брони, надевание старой кожи и возврат позднего комплекта корректно меняют модель и рюкзак. У лучника 55 уровня поздний лук заменён обычным и обратно, без одновременно оставшихся старых мешей. Иконки и характеристики обновляются. 21 сочетание класса/региона дополнительно проверено через реальные GLB и серверные snapshots; материалы соседних героев независимы.
- Примерочная: разные модели четырёх поздних ступеней, цвета качества, бонусы сета, свечение +0…+9. Старые ступени честно отключают ползунок свечения. В 1280×720 модель, ноги и управление помещаются в видимую область. Браузерная примерка не меняет игровые вещи; кузнец и сохранённая заточка не реализованы.

Начальные паузы патруля ограничены 0,8–2,67 с, вместо роста до 106 с по ID моба. Тест всех полевых животных вновь требует хотя бы пять полноценных прогулок за 120 с, без коротких рывков и мерцания Walk/Idle.

Начальный баланс: настоящий `World`, 15 сочетаний класса и уровня 40/55/70/85/99, медианные зелёные вещи. Урон обычных поздних мобов снижен с 62–175 до 34–102, чтобы продолжить пустоши без резкого скачка. Одиночный противник убит всеми классами за 3,2–9,3 с. AFK на краю шести мобов с тремя базовыми зельями не обеспечивает долгую выживаемость всех классов; подробные смерти и ограничения сохранены в [PROGRESSION_100](docs/PROGRESSION_100.md). Это не подтверждение равного PvP, окончательного баланса боссов или 90 дней удержания.

Профиль симуляции: сопоставимый A/B с 16 AFK и 711 спавнами сократил p95 тика 191,30→23,03 мс при одинаковом результате боя. Финальный отдельный прогон после исправления патруля дал p95 8,31 мс; фоновая нагрузка отличалась. Рендер, сеть и БД не входят в эти числа. [Методика и ограничения](docs/WORLD_CPU_AUDIT.md).

Общий сервер 4732, его герои и production не переключались. Push/deployment этого пакета не выполнялись. Новые резервные копии и отдельная сверка обычных героев не проводились по указанию пользователя. Реальный Google OAuth остаётся отдельной настройкой; локальные fixture-cookie не подтверждают настоящий вход Google. Датированные записи ниже — история предыдущих этапов.

## Локально: сборки, Пепельные пустоши и три ранга снаряжения — 16 сентября 2026

Три параллельные ветки сервера/интерфейса/локации интегрированы в `main`, вместе с региональными предметами и анимациями координатора. Финальный `npm run check` — 72 модуля; `npm test` с Node.js 24 — **282/282**, 0 пропусков, около 64 с. Это включает настоящие PostgreSQL/WebSocket, миграцию 5→6, запись/перезапуск билдов, ревизии, канал/DoT/перемещения/бафы, 36 навыков, реальные GLB, классовый/уровневый дроп трёх регионов и неизменность случайных значений после подбора/загрузки. Последний полный прогон содержит исправление точки выхода луча, интерфейс коллекций и актуальные 292 спавна.

Изоляция: новый временный PostgreSQL-процесс на этом Mac не запустился из-за ограничения системной shared memory. Проверки использовали отдельную ограниченную QA-роль, `ashen_test_admin` и случайные базы с префиксом `ashen_test_` в уже работающем локальном процессе PostgreSQL. База обычной игры не открывалась, её таблицы не читались и не менялись. Тестовые базы полного набора удалены штатными test helpers; отдельная браузерная QA-база остаётся для проверки. Закрытые настройки — только в игнорируемых `artifacts/skills-integration/`. Системные лимиты и процесс обычного PostgreSQL не изменялись.

Браузерная проверка объединённого клиента на `localhost:4841`:

- Книга K: назначение луча, drag «Источник маны» во второй слот, вложение восьми обычных очков и одного ключевого таланта, серверное применение, актуальные MP в HUD. Сохранение и загрузка пресета «Охота», перезагрузка страницы, K/Escape подтверждены. Открытая книга сохраняет AFK; только принятое Apply останавливает его. Проверен также viewport 390×844.
- Экспортированные модели в мастерской: прыжок воина с поджатыми ногами, перекат лучника вокруг таза, непрерывная поза луча мага. Луч начинается у правой кисти; точка контакта прыжка совпадает с приземлением. Автоматические проверки проходят все 36 действий на реальных GLB, контролируют хват лука, конечность деформаций, отсутствие погружения и лимиты эффектов.
- Примерочная: обсидиановый воин, снежный охотник и редкий пепельный провидец. Видны реальные PNG, диапазоны и требования 10/25 уровня, смена регионов/класса корректно обновляет коллекцию. Новая внешность — варианты материалов существующих моделей, не отдельные скульпты. Тесты отдельно проверяют восстановление исходного материала и независимость соседнего героя.
- Герой 25 уровня реально прошёл ЛКМ из восточного снега в безопасный пост пустошей и обратно; заголовок, окружение, миникарта и доступность действий сменились по серверному региону. Четыре новых вида предварительно осмотрены в статической мастерской, пути/спавны/геометрия и циклы проверены тестами.
- В общей игре просмотрена стая скарабеев: корректные ноги/тени/атаки, реальные попадания (40 урона), консоль 0 ошибок/предупреждений. Тестовый маг с нераспределёнными очками погиб до ответного AFK-боя и вернулся в лес; этот короткий сценарий не подтверждает длительную охоту или баланс спота. F в безопасном лагере включил ожидание. Долгие AFK/мультиплеерные FPS-прогоны в этот этап не входили.

Воспроизводимый численный аудит: `scripts/balance/skill-alpha.mjs`, 60 основных замеров и шесть проб талантов, без HTTP/БД. За 300 с без зелий лучшие результаты против одной неподвижной цели: воин 259 / лучник 226 / маг 207 DPS; против четырёх плотных целей: 361 / 306 / 396. Метод не учитывает входящий урон и преимущество дальности. Это начальный PvE-баланс, не подтверждение равного PvP или трёхмесячной экономики. [Условия и таблицы](docs/SKILL_BALANCE_ALPHA.md).

Общий сервер 4732 повторно отвечает `/health` 200 со schema 4, не переключался. Production не изменялся; push/deployment не выполнялись. Резервные копии и проверка сохранности обычных героев пропущены по указанию пользователя. Реальный Google OAuth всё ещё требует настройки; локальные тестовые cookie не являются проверкой входа Google. Все новые исходники и документы закоммичены локально, 240 относительных ссылок в затронутых Markdown проверены без отсутствующих целей.

## Production: зелья и AFK — 16 сентября 2026

Опубликован `20260916-consumables-afk-01`, runtime `7b5a7b7`. `npm run check` — 57 модулей, полный набор в новом runtime-образе с изолированной PostgreSQL 18.6 — **212/212** без пропусков; GitHub CI успешен. Тестовая база удалена после прогона, production для нагрузки не использовался.

После штатного завершения старой игры (ExitCode 0) новый контейнер запущен с прежней БД и миграцией до schema 4. По прямому запросу пользователя резервная копия, восстановление дампа и проверка сохранности production-героев **не выполнялись**. Совпадение пользовательских данных этим релизом не заявляется.

Проверены публичный авторизованный WSS-снимок с новым контрактом расходников/AFK, ping/pong, 21 совпадающий с локальной сборкой ресурс и семь закрытых путей (404). Caddyfile и ID/время старта 10 соседних контейнеров, включая PostgreSQL, совпали; игра и шесть остальных HTTPS-адресов — 200. Указатель current и runtime SHA прочитаны обратно. Полная браузерная визуальная проверка относится к идентичному локальному клиенту ниже; на production новая визуальная сессия не проводилась. Артефакты — `artifacts/deploy-20260916-consumables-afk-01/`, не входят в Git.

## Локально: зелья, неподвижный AFK и интерфейс — 15 сентября 2026

Пакет объединён в `main`, без push/deployment. Координатор выполнил `npm run check` и полный набор 210/210 на объединённом коде зелий. Затем браузер выявил неверный payload ручных навыков: точка курсора отправлялась для всех навыков и отклонялась сервером у десяти навыков без области. Исправлен клиентский сетевой метод; его ветка прошла полный набор **212/212**, включая реальные пакеты всех 12 навыков и серверное принятие. После интеграции финальные `npm run check` (57 модулей) и сборка прошли. Стресс-тест ждёт до 5 секунд только штатный `/health` 503 с writer=true/pending=true, не скрывая потерю writer.

Браузерная проверка — отдельный порт 4839 и временная PostgreSQL, пользовательские герои для боя/покупок не использовались:

- Физический клик по шестерёнке AFK до исправления попадал в canvas; после pointer-events:auto открывает настройки. В безопасной зоне AFK включается и ждёт, сохранение порога HP его не выключает.
- Панель кабана показывает только имя, HP и четыре значка добычи по 24 px. Видимого текста у значков нет; названия/редкости/шанс остаются в title и aria-label.
- Привязанный стартовый доспех снят и продан ПКМ: предмет исчез из рюкзака, золото 1000→1008.
- В рюкзаке видны реальные стопки HP и MP, по 3 бутылки. Перетаскивание HP на W и MP на Q поменяло назначения без расхода и удаления стопок; раскладка сохранилась после перезагрузки.
- Подсказка HP показывает восстановление 45 HP, количество/предел и назначение; остаётся видимой после очередных обновлений состояния. Отдельные счётчики панели видны в обычном окне и при viewport override 960×720; прежний CSS для HP больше не переопределяет оформление значка. Override после проверки снят.
- W с назначенным HP восстановила здоровье 76→121 и уменьшила стопку 3→2. Q и автоматическое применение использовали назначенную ману; при открытом инвентаре AFK продолжал бой, а количества в рюкзаке и панели совпадали. После исчерпания показан 0, привязка сохранилась; покупка по одному HP/MP восстановила оба назначения без нового перетаскивания. Смерть не пополняла запас.
- После исправления payload ручной Вихрь принят сервером: MP 78→66, волк погиб и выдано 12 XP. Многочасовой AFK/баланс этим коротким сценарием не подтверждались; неподвижность, радиус и последовательные пустые волны дополнительно покрыты серверными тестами.

Перед обновлением общего 4732 `/health` показывал 0 игроков. Старый процесс штатно остановлен, создан закрытый `backups/postgres-2026-09-15T19-52-30.214Z.dump` (0600, 61125 байт). Проверка миграции 3→4 сравнила **7 таблиц, 73 прежние колонки и 529 строк**: сохранились 1 герой, 2 вещи, 519 операций и все прежние ревизии/поля. Точные старые 3 HP и 3 MP стали двумя стопками, Q/W получили прежние виды; переполнения у этого героя нет. Рабочий сервер запущен из основной папки, `/health` = 200, schemaVersion=4, writer=true, pending=false. Этот локальный дамп отдельно не восстанавливался; восстановление/миграции проверяются тестами временных БД. Production остаётся `20260915-camp-afk-01`, schema 3.

## Production: лагерь, медведи и AFK — 15 сентября 2026

Релиз `20260915-camp-afk-01`, runtime commit `76a38ec`, отправлен в GitHub и опубликован. [CI runtime commit](https://github.com/igorbilogubov/AshenFrontier/actions/runs/35010892589) успешен; все **194/194** теста прошли также в новом Docker-образе с отдельным PostgreSQL 18.6, без пропусков (около 74 секунд). Серверная нагрузка выполнялась только на изолированной тестовой БД.

После штатной остановки игры с ExitCode 0 создан закрытый дамп `/opt/ashen-frontier/backups/postgres-20260915T190536118602Z.dump` (0600). Он восстановлен в отдельную временную БД и мигрирован 1→3. Количество и хеши старых колонок шести таблиц совпали: 5 героев, 17 экземпляров предметов, 17 позиций, 18 идентичностей, 14 бросков и 3440 операций. Та же проверка прошла после миграции рабочей базы и до запуска новой игры. После запуска активные игроки переподключились и продолжили изменять свой прогресс.

Через публичный WSS два прежних QA-героя восстановили 18 полей постоянного прогресса. Проверены взаимная видимость, лес/Стадиум 71/24 моба, оба портала и вход с сохранённой позицией Стадиума. Прежний доспех снят и помещён в сундук; изменены настройки AFK. После нового входа те же предмет/настройки восстановились; затем доспех возвращён в экипировку, настройки — к исходным. Финальные 18 полей совпали с исходным снимком до deployment. Для этого сценария новые герои не создавались.

Шестнадцать публичных HTML/JS/CSS/SVG/GLB совпали побайтно с локальной сборкой; семь закрытых путей/стресс-API — 404. `/health` сообщает PostgreSQL schema 3, writer=true. `current`, image и runtime SHA прочитаны обратно. Caddyfile, ID/время запуска девяти соседних контейнеров и PostgreSQL не изменились; игра и шесть соседних HTTPS-адресов ответили 200. Временные контейнеры/БД проверки удалены. Артефакты: `artifacts/deploy-20260915-camp-afk-01/`, исключены из Git.

Визуальная проверка идентичного клиента выполнена локально в сценариях ниже; новой полной визуальной сессии в production не проводилось. Эта выкладка не подтверждает многочасовой AFK, баланс экономики или вместимость сервера под нагрузкой.

## Лагерь, медведи, AFK и интерфейс — 15 сентября 2026

Интеграция в локальном `main`, без push/deployment. `npm run check` — 54 модуля, `npm test` — **194/194**, без пропусков, Node 24 и временная настоящая PostgreSQL. Проверены серверные попадания/дальность/LOS, дальнее преследование и возврат мобов, безопасная зона, новые спавны и связность дорог, темп навыков, сундук, расходники, фильтры/порядок AFK, миграции 1→3 и 2→3, сетевое сохранение и переподключение. Дополнительная проверка импортов собранного клиента исключает runtime-ссылки на серверные файлы вне public.

Браузерная проверка выполнена на отдельном сервере 4839 с временной PostgreSQL и специально созданными героями; пользовательские локальные/production-герои не использовались для боя и покупок.

- При открытии инвентаря и характеристик DOM-координаты, display и opacity HUD, чата, панели цели, карты, квеста и C/I совпали с исходными. Фокус остаётся на сцене; клавиша 1 использовала навык при обоих открытых окнах (MP 118→110 с учётом регенерации). Escape закрывает окна, не отключая автоохоту.
- На медвежьем споте AFK оставался включённым с открытым инвентарём и затем характеристиками: XP 32→288, золото 1000→1189, рюкзак 1→2. После ручного отключения незащищённый герой погиб; это ожидаемое правило, не бесконечная неуязвимая AFK-сессия. Многочасовой баланс не измерялся.
- Настройки: отключение белых вещей, перестановка Вихря перед Рассечением и HP-порог 55% подтверждены сервером и прочитаны после перезагрузки. Исправлено сохранение числового поля по событию input.
- В доме видны пол, кровать, открытая дверь и личный сундук; крыша и ближние стены скрываются внутри. ПКМ переместила доспех из рюкзака (1→0) в сундук (0→1), перетаскивание вернуло тот же предмет и значения +3 защиты/+10 HP. Реальный подход через дверь проверяется также тестом общей геометрии.
- В магазине куплены HP и MP: счётчики 3→4, золото 1000→994→986. Нет бесплатного пополнения при смерти/возврате в лагерь.
- ЛКМ по торговцу и второму сетевому игроку показывает их имя по центру и кольцо у ног; игрок отображает 90/90 HP. ЛКМ по волку показывает 60/60, 10% вещи, золото и зелёные категории оружия/брони/аксессуаров. Проверен custom SVG cursor и корректный MIME.
- Обычное окно, overrides 960×720 и 390×844: компактный чат раскрывается отдельно от кнопок боя, горизонтального переполнения нет. На узком телефоне док занимает два ряда; это проверка браузерной раскладки, не готовое сенсорное управление. Размер браузера после проверки восстановлен.

Общий локальный сервер 4732 перед началом не работал. Перед его запуском сделан закрытый дамп `backups/postgres-2026-09-15T18-48-51.745Z.dump`. После старта `/health` = 200, PostgreSQL schema 3, writer=true, pending=false. До/после совпали хеши всех старых колонок шести таблиц: 1 герой, 2 идентичности/экземпляра, 0 строк бросков, 2 позиции и 519 операций. Новые колонки добавлены без изменения прежних данных. Дамп этой локальной проверки отдельно не восстанавливался; восстановление и миграции проверены автоматическими тестами временных БД. Production остаётся прежним релизом, сведения ниже — история отдельных этапов.


Обновлено 15 сентября 2026. Основная проверка — браузер, сервер и сохранения. Проба Godot удалена из рабочей папки; её результаты ниже сохранены как история.

Текущий набор — 59 тестов. Последний проверенный игровой релиз — `20260915-ts-01` (`8f9c931`); более ранние результаты ниже отмечены как история. Общая точка продолжения: [STATUS.md](STATUS.md), координация задач в одной папке — [WORKFLOW.md](WORKFLOW.md).

## Одна рабочая папка и удаление Godot — 15 сентября 2026

Локальный `main` в `/Users/igorbelogubov/Projects/AshenFrontier` обновлён до `41e3f59` через fast-forward: TypeScript, характеристики и стенд FPS сохранены вместе со всей историей. Дополнительный `AshenFrontier-character-stats` удалён; `git worktree list` показывает одну папку. AGENTS/WORKFLOW и связанные документы теперь направляют все чаты в неё.

Перед переносом оба локальных игровых сервера штатно остановлены. Внешний архив `/Users/igorbelogubov/Backups/AshenFrontier/consolidation-20260915-091423.tar` содержит исходное состояние обеих папок без зависимостей, `dist/` и Git-метаданных: SHA-256 проверен для 1838 файлов. В общую папку перенесены и сверены 39 файлов браузерных отчётов. Архив и его manifest имеют права 0600 и в Git не входят.

Объединены 11 героев: пять из основной папки, три из рабочей копии и три из эксперимента. Ключи и ID не пересекаются, исходные записи перенесены без перезаписи. Для шести V3-героев проверено точное восстановление всех постоянных полей; для пяти прежних записей — сохранение идентичности, уровня, опыта и золота при штатной миграции. После запуска сравнение с архивом подтверждает сохранность прогресса, вещей, экипировки, распределённых статов и квестов всех 11 героев.

Из рабочей папки удалены проект, скрипты, npm-команды, кеши и экспорты Godot. Сканирование всех файлов, включая игнорируемые и зависимости, без `.git` не находит путей Godot, `.gd`, `.tscn`, `.tres` или `.pck`. Ссылки Markdown на локальные файлы проверены: отсутствующих целей нет. Исторический эксперимент доступен через Git и внешний архив.

В единственной оставшейся папке выполнены чистый `npm ci`, строгая проверка TypeScript, синтаксис 16 JavaScript-модулей и **59/59 тестов**, включая сеть и сохранения. Сервер на 4732 запущен из этой папки; `/health` сообщает `ok: true`, версию сохранений 3 и подключённого игрока. После reload в браузере восстановился прежний маг: уровень 1, 8 золота, 12/65 XP, четыре свободных очка; сцена и HUD отображаются. Нового сравнения FPS не проводилось. Push, deployment и нативные сборки не выполнялись.

## Архивная проба Godot — 14 сентября 2026

Отдельный клиент в `godot-prototype/`, Godot 4.7.2.stable.official, Compatibility. Использует существующие GLB/протокол 2, отдельный Node backend 4733, Web 4741, данные `data/godot-prototype/`. Основная игра 4732 не заменялась. 15 сентября исходники, инструменты и сборки этой пробы удалены из рабочей папки; описание ниже — исторический результат, команды больше не доступны. Исходный код остался в коммите `15a7b9e`.

- `npm run check`: 23 JavaScript-модуля. `npm test`: 52/52.
- Импорт пяти канонических ассетов и release-экспорт Web/macOS выполнены. Исходные Blender/GLB и серверная симуляция не менялись.
- `npm run godot:test`: реальный временный сервер, отдельная сессия Godot, семь мобов, семь клипов героя, изменение реальной позы стопы во время ходьбы. Проверены удержание/отпускание ЛКМ, отсутствие движения/поворота от наведения и WASD/стрелок, остановка над HUD и при потере фокуса, Shift, фиксированный ракурс и колесо.
- Тест проходит путь к волку, удерживает атаку до серверной награды, затем проверяет переподключение и полное пересоздание клиента с чтением записанного ключа. Сохранённые XP/золото и идентичность совпадают. Headless-результат не является измерением графики.
- Регрессия материалов: вершинная окраска `Layered_Fur` включена у всех животных. Отдельно проверяется триангуляция жидкости колб на 10 001 значении от 0 до 1; при почти нулевом HP устранён вырожденный полигон.
- Web просмотрен в живом браузере: читаемые панели, красная HP слева, синяя MP справа, персонаж и окрашенные звери, движение удержанием, бег, приближение, анимация удара, телеграф и потеря здоровья. Убийство волка выдало **12 XP и 8 золота**. После смерти герой вернулся в лагерь. После штатного рестарта тестового сервера и reload клиента эти награды остались.
- Отдельное macOS-приложение запущено и просмотрено. Web и macOS одновременно показывали **двух игроков**; это проверка совместимого входа и общего мира, не испытание вместимости.
- Web-наблюдения менялись примерно от **17 до 60 FPS**. Один замер после прогрева дал p95 переданного в `_process` delta **16.67 мс**, viewport Godot 960×937, два игрока и семь мобов. В ранней нативной сессии при параллельной работе клиентов/захвате окна p95 этого delta достигал **144.44 мс**. Это показания игрового цикла, не GPU-профилирование или независимое измерение времени кадров. Причина разброса не изолирована; нативная производительность не подтверждена стабильным замером.

Окружение, освещение, шейдеры, разрешение и обработка анимаций отличаются от Three.js. Эти результаты **не доказывают превосходство одного движка**. Предсказание под задержку сети, все три класса в интерфейсе, статы/инвентарь, чат, мобильные платформы и нагрузка не проверены в Godot. Для решения о миграции нужен отдельный сопоставимый прогон. Push и deployment не выполнялись.

## Автоматические проверки — 14 сентября 2026

На Node 24: `npm run check` проверяет синтаксис 19 JavaScript-модулей; `npm test` — 52 успешных теста.

- Миграция трёх классов: личность, уровень, деньги и вещи сохраняются, позиция заменяется лагерем новой карты.
- Версия 3 сохраняет здоровье, ману, распределённые очки, ревизию, координаты, откаты, смерть и переполненные награды. Миграция версии 2 сохраняет 3D-прогресс и выдаёт соответствующий уровню бюджет однократно.
- Сервер ограничивает скорость, столкновения, дальность, частоту атак и поддельные пакеты; просроченный ввод останавливается.
- Босс общий, два участника получают личный прогресс/добычу; содержимое чужих рюкзаков и секреты не раскрываются.
- Квест не выдаёт награду повторно; шесть слотов, продажа и полная сумка сохраняют целостность вещей; попытки смены класса не меняют героя, статы или снаряжение.
- Стрелы и магия имеют серверные траектории/столкновения; мобы предупреждают об атаке и возвращаются домой.
- Реальные HTTP/WebSocket-клиенты: движение видно другому игроку, дублирующий/неверный ключ отклоняется, совместный бой, переподключение и штатный рестарт сохраняют данные.
- Закрытые файлы и старый клиент возвращают 404; GLB имеет правильный Content-Type, чужой Origin отклоняется.
- GLB героя/животных: структура скелетов и клипов, конечные координаты, независимые экземпляры, циклы походки и фазы атаки.
- Отдельная регрессия: числовая сила удара не подменяет объект атаки и не ломает предсказание позиции героя.

## Неизменяемый класс героя — 14 сентября

Класс выбирается только при создании. В локальной ветке `codex/character-stats` удалены выбор класса у костра и выдача нового оружия/возврат очков при его смене. Прежняя команда `class` отклоняется сервером. Исторические сценарии смены класса ниже относятся к предыдущей версии.

На Node 24 проходят **52/52** теста и проверка синтаксиса **19 модулей**. Обновлённые тесты проверяют запрет для всех трёх классов в лагере, вне лагеря, при смерти/атаке/бое и полной сумке, сохранность всех постоянных полей героя, классового оружия и вложенных очков. Реальные WebSocket-сессии подтверждают отклонение команды и приоритет сохранённого класса над `classId` при входе по ключу, переподключении и после штатного рестарта, включая героя версии 2.

Локальный сервер 4732 штатно перезапущен. Сравнение сохранённого прогресса до/после подтвердило неизменность классов, уровней, XP, золота, вещей, экипировки, статов и квестов всех трёх локальных героев. В браузере у существующего мага раскрыта «Подготовка у костра»: доступен сброс статов, выбора класса нет, прогресс восстановлен. Ошибок/предупреждений в журнале вкладки нет; `/health` отвечает успешно с версией сохранений 3. Push/deployment не выполнялись.

## Колбы HP и MP в нижнем меню — 14 сентября

Локальная ветка `codex/character-stats`, preview на порту 4732. `npm run check`: **19 модулей**; `npm test`: **52/52**; `git diff --check` без замечаний. Новые тесты игровой логики не добавлялись: изменение относится к представлению текущего состояния.

В браузере проверены маг на обычном размере 740×723 и отдельный QA-воин с кнопками обоих видов оружия, широкая раскладка 1280×720 и узкая 390×844. Красная колба расположена слева, синяя справа от кнопок, круги не деформированы; горизонтального переполнения нет. Открытие/закрытие характеристик сохраняет доступность нижних кнопок, колбы скрываются на время окна. После вложения пяти очков в энергию тестового воина максимум маны вырос с 40 до 60 без мгновенного заполнения: в колбе видны свободное место сверху, текущие 47/60 и последующая регенерация (следующий DOM-снимок — 49/60, заполнение 82,68%). Полный запас здоровья/маны также проверен. Уровень жидкости использует дробное значение, число запаса округляется как прежде.

В журнале QA-вкладки нет ошибок/предупреждений. Настройка размера сброшена, QA-вкладка закрыта, исходная игра пользователя обновлена. Серверные правила и сохранения не менялись, push/deployment не выполнялись.

## Управление удержанием ЛКМ — 14 сентября

Проверено локально на `http://127.0.0.1:4732/` в ветке `codex/character-stats`. Синтаксис **19 модулей**, полный набор **52/52** и `git diff --check` проходят. Пять новых тестов проверяют текущую сторону курсора, одинаковую скорость по направлениям, отсутствие движения/поворота при наведении без удержания, торможение после отпускания, зону остановки у ног, невалидную точку/смерть, дистанцию атаки и прекращение преследования.

В браузере выполнены перетаскивание с зажатой ЛКМ по земле, смена стороны курсора, отпускание над сценой и над кнопкой интерфейса, нажатия WASD/всех стрелок, клик по миникарте, открытие/закрытие окна героя. Отдельный WebSocket-наблюдатель подтвердил движение браузерного героя во время жеста и нулевую скорость после отпускания; после WASD/стрелок и миникарты координаты и угол не изменились. В заключительных 98 снимках за 5 секунд координаты, угол и скорость оставались постоянными. В журнале вкладки нет ошибок/предупреждений. Наблюдатель отключён, игра оставлена открытой.

Свободное наведение без кнопки проверено автоматическим тестом логики ввода; отдельного действия hover у используемого браузерного инструмента нет. Длительное удержание в бою и отпускание вне окна браузера в этой ручной проверке не воспроизводились. Захват указателя, потеря фокуса/соединения и открытие интерфейса сбрасывают удержание в коде. Подсказки обновлены, точечный маршрут удалён. Push и deployment не выполнялись.

## История: ручные характеристики и новый инвентарь — 14 сентября

Локальная ветка `codex/character-stats`, отдельный сервер `http://127.0.0.1:4732/`, собственный каталог `data/`. Push и deployment для этой работы не выполнялись. После изменений полный набор **47/47** проходит на Node 24, синтаксис 18 модулей и `git diff --check` проверены.

Добавлены проверки четырёх характеристик/трёх классов, начального и уровневого бюджета, реального повышения уровня после убийства, повторных/устаревших/поддельных пакетов, атомарности, ограничений лагеря и боя, сброса/смены класса без лечения, переполненной сумки, миграции версии 2, восстановления версии 3 без повторной выдачи маны, попаданий/промахов ближних и дальних атак, расхода маны, регенерации и уменьшения входящего урона. Отдельный тест с настоящими WebSocket-соединениями проверяет старого героя, распределение, приватность, переподключение и штатный перезапуск.

В браузере проверены:

- Воин: черновик +1 силы/+1 живучести показывает урон 26 → 26,8 и HP 100 → 104; применение тратит два очка, сервер подтверждает, обновление страницы сохраняет результат. Бесплатный сброс возвращает бюджет.
- Лучник: +1 ловкости показывает одновременное изменение урона, точности и защиты; отмена убирает черновик. Маг: +1 энергии повышает урон, максимум маны на 5 и её регенерацию на 0,075/с. Смена класса возвращает вложенные очки.
- Фокус кнопки сохраняется при обновлениях мира. `Q` и `W` при открытом окне не вызывают боевую команду и движение. Кнопки действий учитывают соединение, лагерь и состояние героя.
- Компоновка при фактических CSS-размерах 1067×750 (две панели), 740×723 (вкладки) и 325×703 (компактный режим) проверена. На узком экране нет горизонтального переполнения; панель прокручивается, подтверждение и закрытие доступны. Настройка размера браузера после проверки сброшена.
- Маг в общей локации победил волка, получил 12 XP, 8 золота и посох. Смерть/возрождение и обновление страницы сохранили награды и распределение. Новый инвентарь позволил выбрать «Посох сумерек +5», надеть его у костра; надетую вещь нельзя продать. Видны ровно шесть слотов и 16 ячеек сумки.
- Исправлены перекрытие имени героя чатом после добавления маны, видимость пустых ячеек и точность чисел предпросмотра. В журнале просмотренной вкладки нет ошибок и предупреждений.

Таблица формул для одинакового бюджета на уровнях 1/10/20/60 выявила и помогла убрать чрезмерный рост защиты лучника. Сводка и ограничения расчёта — [PROGRESSION.md](PROGRESSION.md). PvP, AFK и длительное испытание баланса ещё не проводились; рост FPS этой работой не заявляется. Модели и анимации не менялись.

## История: браузерная проверка перехода на общий 3D-мир

Два независимых профиля в общем мире: воин «Дозорный» и маг «Искра». В обоих видны второй персонаж и счётчик двух игроков; перемещение синхронизировано. Маг вышел из лагеря, убил волка, получил 12 опыта, 8 золота, 1/5 задания и «Посох сумерек +5»; надел его у костра. Обновление страницы и остановка/запуск сервера восстановили того же мага с теми же наградами и надетым посохом. Соединения восстановились автоматически. Смерть и возвращение в лагерь не обнулили прогресс.

Проверены широкий и узкий браузерные размеры, интерфейс шести слотов и прокручиваемого рюкзака. После устранения ошибки состояния атаки герой виден, новые ошибки рендера не появляются. Около 59–60 FPS в проверенной сцене с двумя героями — наблюдение, не нагрузочная гарантия.

## Публичная проверка и её история

Для проверки HTTPS/WSS используется `node scripts/smoke-online.mjs <адрес>`: два новых QA-героя, общая видимость, движение и серверная запись. Ключи сохраняются только в закрытом `artifacts/shared-3d/online-heroes.json`. После штатного рестарта только игрового сервиса:

```sh
node scripts/smoke-online.mjs https://ashen-frontier.91.99.21.123.sslip.io --restore
```

Сравниваются id, класс, уровень, опыт, золото, вещи, экипировка, координаты и задача. Эта команда проверяет сохранения текущего протокола, не старый пиксельный мир. При первом 3D-релизе публичные HTTPS/WSS и восстановление двух героев после рестарта проверены успешно. Браузер загрузил 3D-опушку и переподключился; сохранились все 11 исходных героев. К последнему релизу `20260914-3d-03` записей было 15, их основные поля прогресса также сохранились. Девять других контейнеров не менялись, шесть прежних HTTPS-адресов отвечали 200. Подробности в [DEPLOYMENT.md](DEPLOYMENT.md).

В новом clone файла с QA-ключами нет. `--restore` требует ранее созданного закрытого `artifacts/shared-3d/online-heroes.json` для того же адреса. Обычные `npm test` не требуют ключей и не подключаются к production. Отчёты из `artifacts/` остаются локальными; проверяемые сценарии и результаты описаны здесь.

## Не покрыто текущей проверкой

Длительная нагрузка с 16 игроками, слабые устройства, аварийная потеря VPS, AFK-начисления, PvP/PK, длительный баланс ручных сборок, рынок и платежи. Мобы и чат перезапускаются вместе с миром. Сохранения JSON с двухсекундным интервалом ещё не заменяют транзакционную экономику. Аккаунты с восстановлением доступа и отдельные клипы дальних классов предстоит сделать; модели трёх классов добавлены 15 сентября (проверка ниже).

## История: исправление свободной прогулки — 14 сентября, 3d-02

Причина рывков была общей для старых процедурных и новых GLB-мобов: патруль следовал за движущейся точкой с порогом остановки 0,12 м. В двухминутной симуляции без игроков получалось 821–876 переходов движение/остановка в минуту на моба, с типичным рывком около 0,065 с.

После замены на постоянную цель, разгон/торможение, поворот и полноценную паузу: 15,5–21 переход в минуту, непрерывная ходьба в среднем 3,9–5,7 с. Отрезков ходьбы короче 0,2 с нет. Все семь мобов остаются в разрешённой области вне лагеря; агрессия прерывает патруль немедленно, скорость погони сохранена, смерть сбрасывает маршрут. Два регрессионных теста добавлены, полный набор 29/29 проходит. В браузере проверены прогулка у лагеря и отсутствие ошибок рендера. Геометрия и клипы в этом исправлении не менялись.

## Доработка волка — 14 сентября

Обычный волк пересобран в Blender, кабан и вожак совпадают с прежними файлами по SHA-256. У волка 24 кости, 18 154 треугольника, восемь клипов. Все 31 тест проходят: дополнительно проверены опора четырёх лап в шаге, рыси и двух поворотах, замыкание циклов и непрерывность смешанной позы при 30/60 FPS. Проверки атаки, смерти, возрождения, сети и сохранений также проходят.

В бестиарии проверены переключение «до / после» в одинаковой фазе шага и воспроизведение поворота. В локации проверены загрузка нового волка и патрулирование, ошибок рендера нет. В этой сессии браузер показывал около 23–28 FPS в локации; это отдельная проблема производительности, улучшение FPS этой правкой не заявляется. Отчёты и журнал сборки: `artifacts/wolf-polish/`. Подробности модели и ограничения: `art/creatures/ashen-fauna-v1/README.md`.

## Проверка репозитория и параллельной работы

GitHub Actions (`.github/workflows/ci.yml`) запускает `npm ci`, `npm run check`, `npm test` на Node 24 для PR и push в `main`. Workflow не использует production-данные и не выполняет deployment. Для локальной проверки после clone достаточно этих же команд: нужные GLB и отчёт сборки воина входят в Git.

Для ручной проверки используется общий сервер 4732 из единственной рабочей папки `AshenFrontier`. Автоматические и нагрузочные проверки используют отдельные временные данные; второй процесс с общим `data/` не запускается. Проверка в разных вкладках одного сервера использует разные QA-сессии. В отчёте обновляются результаты именно текущей задачи; исторические числа тестов и FPS не подменяют новый результат.

## TypeScript — 15 сентября 2026

Основной игровой код проверяется с `strict`, `noEmitOnError`, `verbatimModuleSyntax`. Клиент, сервер, общие формулы и контракты компилируются в `dist/`; исходники не выдаются HTTP. Все прежние 52 теста проходят на собранных модулях, включая настоящие WebSocket-сессии, миграции V2/V3, неизменяемый класс и сохранение после перезапуска. Перенос языка сам по себе не является доказательством ускорения.

## FPS и повторяющиеся скелеты — 15 сентября 2026

`npm run check` и 59/59 тестов: добавлены проверки длинных зависаний в статистике, закрытости диагностических API, изолированной нагрузки 16 настоящих WebSocket-сессий и очистки ботов. Реальный GLB проверен на идентичность деформированных вершин всех семи клипов после объединения одинаковых скелетов; два героя сохраняют независимые кости/текстуры, разные inverse bind matrices не объединяются. При уходе удалённого героя освобождается его GPU-текстура скелета.

Проведены браузерные измерения в Chrome с аппаратным WebGL2/Metal на M1; полная таблица и ограничения в [PERFORMANCE.md](PERFORMANCE.md). Графика и управление проверены в браузере. TypeScript-миграция не меняет схему V3/протокол2. Push, deployment и новые нативные сборки в этой задаче не выполнялись. Dockerfile переведён на два этапа, но локальный Docker отсутствует, образ не собирался.

Основной локальный сервер перезапущен на 4732; существующий профиль восстановился, окна характеристик и инвентаря с шестью слотами открываются. Отдельный нагрузочный сервер 4742 после замеров остановлен. Встроенный браузер в своей меньшей сцене показывал до 60 FPS; это индикатор другого окна/ракурса, не результат сопоставимого стресс-прогона Chrome 1527×810.

## Hetzner: 20260915-ts-01 — 15 сентября 2026

В production выложен commit `8f9c931` после успешного GitHub CI и 59/59 тестов внутри нового Docker-образа. Все 15 прежних героев прошли предварительную миграцию в памяти; защищённая копия снята после штатной остановки старого контейнера. После обновления 13 основных полей прогресса всех этих героев совпали, два прежних QA-профиля восстановили 28 полей V3 по публичному WSS после контрольного рестарта.

Публичные 12 ресурсов совпали с проверенной сборкой, приватные/диагностические маршруты возвращают 404. В браузере проверены вход, общий мир с другим игроком, модели, колбы, четыре стата, пять начальных очков и шесть слотов инвентаря. Добавлен один отдельный браузерный QA-герой: всего при проверке 16 записей. Это проверка релиза, не стресс-тест и не доказательство стабильных 60 FPS.

Caddyfile, ID и время запуска девяти остальных контейнеров совпадают до/после; все шесть соседних HTTPS-адресов и игра отвечают 200. `current` переключён после проверки. Образ healthy, схема 3, обычное хранилище, `GAME_STRESS` выключен. Подробности, резервная копия и ограничения отката V3 → V2: [DEPLOYMENT.md](DEPLOYMENT.md). Отчёты — `artifacts/deploy-20260915-ts-01/`, вне Git.


## Первая случайная экипировка воина — 15 сентября

Локальная функциональная проверка, не production deployment. `npm run check` и `npm test`: 66 тестов. Новые проверки покрывают включённые границы/шаг случайных значений, множество разных экземпляров, поддельные свойства, сохранность старых/новых вещей и pendingItems, ограничения класса/костра, доступность шести слотов, отсутствие двойного бонуса power, влияние скорости на длительность без сокращения отката. Реальные WebSocket-клиенты проверяют надевание, appearance у наблюдателя и те же значения после рестарта временного сервера.

На экспортированном модульном GLB проверены семь клипов, конечные вершины/границы в стойке, ходьбе, беге, обоих ударах, попадании и смерти, тяжёлый/лёгкий/смешанный/снятый комплект, отдельная палитра другого героя и одна общая палитра всех частей одного героя. Неизвестные head/boots appearance дают базовые части. Позиция головы в Idle через 4 секунды совпала с исходным GLB.

В браузере проверены смена облика в примерочной, бег, новый образец и максимум HP +15 в диапазоне 5–15. Примерка не соединяется с игровым WebSocket и не выдаёт вещи герою. В обычном клиенте изолированного сервера проверены диапазоны/сравнение, прокрутка к описанию, надевание кожаного доспеха и отметка «Надето»; максимум HP изменился с 139 до 136 при замене +13 HP на +10 HP. После перезагрузки клиента значения сохранились. Обычные сохранения не использовались как тестовая база.

Короткий FPS-стенд: M1, Chromium, 1280×720/DPR 1; 5 секунд прогрева + 12 секунд измерения. Смешанное снаряжение 1/4/16 воинов: 60/60/60 FPS, p95 17,6/17,6/18,5 мс, кадров >50 мс нет. Контроль 16 воинов с прежними предметами в том же модульном ассете: 60 FPS, p95 17,7 мс. [Конфигурация и счётчики](docs/equipment-performance-2026-09-15.json). Это не сравнение с прежним бинарным ассетом и не доказательство длительных 60 FPS, производительности любого устройства или вместимости production.

Ограничения: десять пробных основ только воина, без сетовых бонусов/заточки; баланс всех классов не настроен на эти вещи. Тело/голова под снятой бронёй пока упрощены, у кожи есть пересечения с унаследованными пластинами при некоторых позах. Источник/генератор/экспорт сохранены; дальнейшая художественная полировка отдельно.

Общий локальный сервер 4732 перезапущен штатно при нуле подключённых игроков после резервного копирования. Содержимое сохранений до/после перезапуска идентично; `/health` успешен, новый GLB отдаётся с HTTP 200. Изолированный сервер 4742 остановлен после проверки, примерочная оставлена на 4732. Production и остальные сайты не затрагивались.


## Mixamo: три модели и замена кожи — 15 сентября 2026

Локальная версия после первой примерки. `npm run check` успешен, `npm test` — 69/69. Три новых проверки используют реальные экспортированные GLB: отдельные текстурированные тела лучника/мага, одна палитра 67 костей на героя, независимые палитры клонов, семь клипов с конечными границами в девяти фазах и человеческим масштабом Idle. Положение вершин тяжёлых Armor_Body/Helmet/Boots совпадает с архивным Paladin; под кожаным капюшоном остаётся новая голова Exo Gray с UV.

Браузер Chrome: проверены Exo Gray без снаряжения, тяжёлый комплект, новая кожа и бег; Erika Archer и Dreyar в примерочной, выбор класса и соответствующие отдельные модели в общей изолированной локации. При подготовке обнаружен десятикратный размер исходного скелета Dreyar — исправлена нормализация единиц в Blender, повторно проверен именно GLB. Классовый выбор в примерочной не меняет героя или его вещи. Класс обычного игрового клиента берётся из серверного профиля до загрузки ассета.

Четыре коротких замера на `npm run stress`, порт 4742, временные данные: 1 герой — 57 FPS; 16 героев трёх классов — 28,1 FPS в первом прогоне и 41,8 FPS при повторе; без теней — 43,7 FPS. Первый полный прогон содержит кадр 2182 мс. Условия и числа сохранены в [PERFORMANCE.md](PERFORMANCE.md) и [JSON](docs/mixamo-models-performance-2026-09-15.json). Стабильные 60 FPS в толпе не подтверждены. Это другой браузерный режим/разрешение относительно предыдущей примерки; прямое сравнение старых и новых моделей не проведено.

Ограничения: стрельба и каст пока используют прежние временные клипы; лук/посох ещё процедурные. Erika и Dreyar пока имеют цельную базовую одежду. Новая кожа воина подогнана к общему скелету, отдельные пересечения в крайних позах ещё возможны. Лицевая анимация Exo не используется. Реалистичный персонаж не означает окончательно согласованный арт всех классов.

Статические клиентские ресурсы обновлены на общем локальном 4732 без перезапуска игрового сервера. Обычные сохранения и production не менялись. Стресс-сервер 4742 остановлен, временный размер окна браузера сброшен, примерочная оставлена на 4732. Push/deployment не выполнялись.

## Иконки предметов и отдельный рюкзак — 15 сентября 2026

`npm run check` успешен; финальный `npm test` — 74/74. Новые проверки: надевание/снятие не теряют и не дублируют экземпляр; 16 ячеек считаются отдельно от экипировки; полный рюкзак блокирует снятие, но допускает замену; надевание в пустой слот освобождает место для одной ожидающей награды; предмет в последней свободной ячейке не помечается ожидающим. Проверены наличие всех 15 прозрачных PNG 256×256 и отдельная картинка каждой основы воина. Расширена проверка настоящих WebSocket-клиентов и рестарта: надетые исключены из рюкзака, снятое возвращается один раз, свойства и все экземпляры совпадают.

Один промежуточный полный запуск упёрся в прежний 9-секундный таймаут боя с боссом в shared-server; финальный полный прогон после закрытия браузерной тестовой сцены прошёл. Боевой тест и его таймаут в этой задаче не изменялись.

Браузер Chrome, изолированный сервер 4743 и временные герои: шесть вещей в рюкзаке при шести надетых. Замена панциря на кожу сохраняет 6/16, кожа уходит из сетки, панцирь возвращается в неё. Снятие кожи даёт 7/16 и пустой слот доспеха. После перезагрузки остаётся 7/16; та же кожа встречается в рюкзаке ровно один раз. Проверены реальные PNG в слотах/рюкзаке/описании, лук/посох чужих классов, отсутствие ошибок браузера и загрузка каждого показанного изображения. Иконки осмотрены также в примерочной на 4732. Исходные модели/анимации не менялись; отдельный FPS-замер не проводился.

Общий локальный сервер 4732 перезапущен штатно при нуле игроков после резервного копирования. SHA-256 обычного файла сохранений идентичен до/после, health успешен; HTTP-ответы проверенных PNG совпадают с локальными файлами. Изолированный сервер 4743 остановлен, его вкладка закрыта. Все тестовые вещи находились во временном хранилище, обычным героям ничего не выдавалось. Схема сохранений по-прежнему V3. Push и deployment не выполнялись.


## Общая интеграция экипировки, навыков и спотов — 15 сентября

`npm run check` успешен, `npm test` — **109/109** на объединённой версии. Ветки агентов (`codex/class-equipment`, `codex/class-skills`, `codex/forest-spots`, `codex/combat-review`) объединены локально в `main`. Тестовые ключи/сохранения и FPS-отчёты не публикуются; production не использовался.

Проверены серверные ограничения шести навыков, урон/предел целей/точность/препятствия, MP/откаты и отсутствие повторного попадания снаряда. Настоящие WebSocket-пакеты передают публичный каст и эффект второму игроку; приватные мана/откаты и существующие вещи переживают перезапуск временного сервера. Новый каталог каждого класса даёт шесть последовательных слотов, сохраняет точные броски и публикует только appearance другим игрокам. Переполнение рюкзака, переодевание и наследованные предметы покрыты регрессиями.

AFK проверен через World и реальные соединения: старт только на споте, нейтральный ввод не отменяет, ручное действие/смерть/разрыв соединения отменяют; после загрузки режима нет. Автоматический вклад не выполняет сюжет, очередь ограничивает дальнейшую автоохоту, мобы остаются в споте и возрождаются через 16 секунд. 60-секундная симуляция стартовых вещей показывает риск смерти — детали в docs/AFK.md, это не доказательство долговременного равенства классов.

Реальные GLB экипировки и библиотеки Mixamo проверены во всех шести навыках (61 фаза): конечные вершины, отсутствие ухода корня/взрыва меша, сброс после атаки/смерти. Браузерная мастерская показала натяжение лука со стрелой, выпуск, одноручное и круговое заклинания. Дополнительная проверка трёх GLB мобов покрывает все клипы и 25 фаз, включая выпад/смерть, внутри постоянных границ отсечения.

В Chrome проверена сцена с новыми Q/E, расходами MP и отдельными откатами, а также правый клик поверх открытого инвентаря: браузерное контекстное меню не появляется. Исправлен найденный визуальный дефект: пул эффектов больше не поглощается объединением статического окружения, белого кольца у лагеря нет. Два лесных спота и четыре коллекции дополнительно осмотрены агентами в отдельных браузерных мастерских; их доказательства и ограничения описаны в docs/FOREST_SPOTS.md и docs/CLASS_EQUIPMENT.md.


На временном стенде браузер показал «Автоохота включена», текущие HP/MP и круг спота. Кнопка остановила режим, отдельные Q/E списали MP и показали свои откаты (3,8 с / 4,8 с сразу после нажатия), F снова включила автоохоту. Автоматические убийства оставляли сюжет 0/5; ручное убийство после остановки увеличило его до 1/5. Воин с шестью тестовыми предметами виден в самом споте; границы деревьев/камней и телеграфы не пропадают. Широкий HUD расширен для новых кнопок, на узких экранах сохраняется перенос.

Общий сервер 4732 штатно перезапущен при нуле подключённых/живущих в мире героев. До этого сделана игнорируемая резервная копия; после запуска **все 12 записей локальных героев в точности совпали с копией**, `/health` — ok, V3. Это проверка локальной установки, без отправки изменений на GitHub/Hetzner. FPS-стенд и дополнительный сервер 4744 после проверки остановлены; полные числа, включая плохие повторные результаты плотной толпы, сохранены в PERFORMANCE.md.

## PostgreSQL, открытый мир и интерфейс — 15 сентября 2026

Интеграционный набор после карты, добычи/торговца и хвата лука: `npm run check`, `npm test` — 136/136. Тесты используют отдельные временные базы PostgreSQL. Проверены реальные WebSocket-покупки/продажи, точные предметы и сохранение после рестарта, ручной подбор, владелец добычи/препятствия/полный рюкзак, четыре спота, immutable rolls, ревизии и идемпотентность DB-пакетов. Принудительное отключение writer-сессии даёт 503 и закрывает игровые соединения; неподтверждённая продажа не публикуется, рестарт восстанавливает последний commit. Исправлена изоляция теста advisory lock: выбор PID ограничен OID собственной базы, иначе параллельный тест мог завершить чужой writer.

Браузер Chrome, изолированный сервер 4734 и временные герои:

- При LMB по подписи NPC сервер открыл магазин и инвентарь. Три вкладки, шесть товаров класса; картинки предметов соответствуют моделям. ПКМ купил меч за 40 (500 → 460), ПКМ в рюкзаке продал его (→ 477). Проверены также покупка посоха перетаскиванием и продажа сапог переносом в магазин.
- ПКМ вне магазина надевает капюшон, он исчезает из рюкзака и появляется в слоте. Перенос капюшона обратно снимает, перенос в пустой слот надевает. Использован pointer drag: исходный HTML drag в Chrome не доводил перенос до пустого слота. Подсказка появляется при наведении/клавиатурном фокусе без выбора; видны все значения, диапазоны и сравнение.
- Рюкзак: 16 ячеек, около 84,6 × 84,6 CSS px на широком экране; белые и зелёные рамки осмотрены. Синий стиль соответствует третьему существующему tier. Размеры окна фактически 1527 × 809 и 745 × 673 CSS px (viewport override масштабируется Chrome). При узком открытом инвентаре C/I не перекрывают ячейки. `artifacts/current-ui/compact-shop.png` — локальный снимок.
- Настоящее убийство временным героем оставило 8 золота на земле, не в кошельке. В браузере LMB по названию запустил подход, кучка исчезла, кошелёк стал 8. Сетевой тест отдельно проверяет сохранение и повторный подбор без дублирования.
- Интегрированный лучник загружен без ошибок консоли: лук в ладони, отдельные классовые иконки. [720 кадров и визуальный осмотр обоих луков](docs/BOW_GRIP.md). Повтор ПКМ проходит через тот же цикл атаки, что Space; сброс удержания предусмотрен на отпускание, уход с canvas, отмену/focus/death/panel.
- Резервный дамп восстановлен в отдельную PostgreSQL-базу; три тестовых героя, предметы/броски/экипировка/золото/ревизии совпадают. Общий сервер 4732 затем перезапущен с чистой PostgreSQL; 8 прежних JSON-файлов заархивированы, контрольные суммы остались прежними. Production не обновлялся.

Результаты FPS записываются отдельно в PERFORMANCE.md. Скриншот обычной игры и прохождение тестов не доказывают стабильные 60 FPS, длительный AFK, слабые устройства или production-нагрузку.

После исправления стресс-подключений финальные `npm run check` и `npm test` прошли: **137/137**, без пропусков. Новый сетевой сценарий выполняет 1 лагерь → 16 AFK → 1 лагерь → 16 AFK на настоящей изолированной PostgreSQL; повторный профиль получает новые ID экземпляров, наземная добыча очищается при смене сценария.

Финальный браузерный стресс-прогон подтверждает 16/16 активных AFK, 16 навыков и 16 атак за 12 секунд: 36,5 FPS; одиночный лагерь — 42,5 FPS. Стабильные 60 FPS не достигнуты; условия, плохие хвосты серверного тика и исключённый ошибочный AFK-сценарий описаны в [PERFORMANCE.md](PERFORMANCE.md). После проверки временные серверы/кластеры остановлены, тестовая вкладка закрыта; общий сервер 4732 остаётся с PostgreSQL и успешным health.


## Production PostgreSQL: 20260915-pg-01 — 15 сентября

По команде пользователя опубликован `96374e9`. GitHub CI успешен, полный повтор 137/137 внутри runtime-образа на изолированном PostgreSQL. Проверены публичный WSS двух героев, взаимная видимость/движение и совпадение 26 полей после штатного рестарта. Одиннадцать ресурсов совпали побайтно; дамп с двумя героями восстановлен отдельно, все семь таблиц совпали по количеству/хешам строк. Caddy и девять других контейнеров не изменились; шесть соседних адресов HTTP 200. Детали, пределы backup и отличие от разрабатываемых четырёх навыков/Стадиума — в DEPLOYMENT.md.

## Четыре навыка, Стадиум и интеграция клиента — 15 сентября

Финальные `npm run check` и `npm test`: **162/162**, без ошибок и пропусков, runner 35,5 с. Полный лог — `artifacts/stadium-integrated-final-test.log`. Включены прежние PostgreSQL/экономика/сохранения и новые проверки классов, попаданий/предела целей, двенадцати GLB-действий, замедления, предупреждений/отложенных областей, молнии, трёх загонов, порталов, сетевой изоляции областей и восстановления сохранённой позиции. Тесты используют временные PostgreSQL-базы, не обычный мир.

Промежуточные прогоны выявили две проблемы и не считаются успешными результатами: проверка HP ожидала ответ зелья фиксированные 80 мс; заменено ожиданием серверного расхода зелья и верхней границей естественного регена. При входе во время асинхронного сохранения сервер иногда молча оставлял сокет без ответа; теперь возвращается `storage_unavailable`, после чего клиент повторяет вход. Новая сетевая проверка синхронизирует это пересечение в тестовой БД, подтверждает отсутствие созданного бесхозного героя и успешный повтор. После уже подтверждённого создания unrelated checkpoint не отменяет выдачу ключа.

В Chrome на изолированном 4734 проверены:

- ЛКМ по подписи портала в лагере → серверный подход → безопасная площадка Стадиума; обратный портал возвращает в лагерь. Меняются заголовок, миникарта/маркеры и подсказки; старая цель/ввод/эффекты не переносятся.
- Вход по сохранённой позиции сразу открывает Стадиум. Во время тестовой смерти лучника камера и интерфейс вернулись в лесной лагерь. Лесной квест не получил зачёт от убийства кабана в Стадиуме.
- Все Q/E/Z/X трёх классов: корректные названия/иконки, запуск клавишами, расход MP и отдельные серверные откаты. У лучника Q/E дали реальный kill/XP и личное золото на земле; Z/X проверены отдельным экземпляром на дорожке. Вверх направленный лук дождя и разные жесты мага/воина видны в игровой камере. Подробные фазы и два физических лука дополнительно проверены в мастерской/GLB-тестах агента.
- Все три загона есть на миникарте; кабанья группа/ворота видны в игре. Полная геометрия трёх загонов/18 моделей проверена отдельным арт-стендом, AFK всех девяти сочетаний класс/загон и повторное появление мобов — серверными тестами.
- При viewport 600 × 850 шесть одинаковых клеток идут в два ряда, измеренная ширина каждой 87 CSS px; колбы стоят по сторонам, C/I доступны, подсказка не выходит за панель. Override снят. Ошибок/предупреждений игровой консоли при проверке нет.

Материалы — `artifacts/stadium-integrated/`; агентские пошаговые изображения — в worktree анимаций. Частота из обычного HUD не считается воспроизводимым FPS-тестом. Длительная нагрузка новой арены и равенство классов в PvP не подтверждены.

После проверки временный сервер 4734 и его PostgreSQL остановлены, база удалена, вкладка закрыта. Общий 4732 штатно перезапущен из main при нуле игроков после `backups/before-stadium-20260915.dump`; health PostgreSQL/writer успешен. Количество и хеши всех строк семи таблиц до/после совпали. Сохранения не заменялись. Новые навыки/Стадиум остаются локальными коммитами, production по-прежнему `20260915-pg-01` (`96374e9`).

## Подготовка deployment навыков и Стадиума — 15 сентября

На изолированном PostgreSQL 18.6 внутри нового runtime-образа первый серверный прогон дал 161/162: AFK-сценарий иногда принимал запоздавшее движение из предыдущего сценария; отдельный повтор прошёл. Исправлен сам стресс-контроллер: во время смены сценария ботам запрещена отправка новых действий, нейтральный пакет подтверждается серверным ack до сброса героев, номера команд остаются монотонными. Проверяется реальное прохождение WebSocket, без фиксированной задержки. Производственный игровой ввод не изменён. После исправления локальные `npm run check` и полный `npm test`: 162/162, без пропусков. Логи в `artifacts/deploy-20260915-skills-stadium-01/`; итог серверной проверки и выкладки записывается отдельно после выполнения.

## Deployment навыков и Стадиума — 15 сентября

Релиз `20260915-skills-stadium-01`, runtime `c601124`. После исправления стресс-барьера весь серверный прогон: **162/162**, без пропусков, около 67 секунд; это тестовый контейнер с отдельной PostgreSQL, не рабочая нагрузка. Новый образ проверен до переключения. Дамп 17:23:21 UTC восстановлен в отдельную БД, хеши всех семи таблиц совпали. При старте активный игрок переподключился и появилась новая штатная запись журнала, поэтому не заявляется полное равенство всех строк после запуска: отдельно совпали 19 полей прогресса всех четырёх героев, а также предметные таблицы.

Два прежних QA-профиля восстановили 26 полей по публичному WSS. Проверены взаимная видимость, изоляция леса/Стадиума, 41/18 мобов, круговой переход порталов и вход с сохранённой позицией Стадиума. В браузере опубликованный лучник показал четыре навыка с иконками, прошёл портал; ошибок/предупреждений консоли нет. Двенадцать публичных ресурсов совпали побайтно, закрытые пути/стресс-API — 404. Caddyfile, девять соседних контейнеров и PostgreSQL сохранили ID/время запуска; семь HTTPS-адресов ответили 200. `current` указывает на новый релиз. Push в этой задаче не выполнялся. Артефакты и screenshot: `artifacts/deploy-20260915-skills-stadium-01/` (закрыты/исключены из Git).
# Снежная область и элиты — 16 сентября 2026

После объединения аккаунтов, мира, моделей и окружения `npm run check` успешен, полный `npm test` — **246/246**, без пропусков, 31,8 с. После расширения бестиария и завершающей полировки снега повторены `npm run check` (64 JavaScript-модуля), строгая сборка и полный `npm test`: **246/246**, 64,5 с при параллельной браузерной проверке. Изменения остаются локальными.

Новые сценарии проверяют три площади леса, 98 снежных спавнов и проходимость, границу 9/10 уровня, переход при удержании ЛКМ и через команду, невозможность подмены уровня/позиции/золота, изоляцию игроков/мобов/атак/наград по локации, обратный путь. Реальные WebSocket с account-cookie и временной PostgreSQL сохраняют точную снежную позицию, экземпляр синего предмета/броски/героя/золото после перезапуска игрового процесса; процесс PostgreSQL не перезапускался. World-тесты покрывают 3% абсолютного синего дропа, более редкий респаун именных элит и неподвижный AFK начального снежного вида тремя классами на уровне 10.

Реальные GLB проверены по конечным вершинам, опорным конечностям, замыканию циклов, смерти и бюджетам; анатомия паука/голема проверяется отдельно от четвероногих. Все 30 синих определений разрешаются в существующую PNG именно их облика. Клиентские проверки подтверждают плоскую игровую поверхность, 240 частиц и отключение снежной сцены вне региона, настоящее HP/редкость элиты. Промежуточные ошибки касались старого ожидания спавна без eliteId, теста с двумя разными экземплярами Three.js и неполного DOM-mock; исправлены, проверка поведения сохранена.

Ни общий 4732, ни production этой проверкой не мигрировались/перезапускались. Игровые герои, вещи и cookie браузерного стенда принадлежат отдельной базе `artifacts/account-qa/`; реальный Google OAuth остаётся отдельным непроверенным этапом настройки. FPS под массовой нагрузкой и длительный баланс снежной области этим прогоном не подтверждаются.

Браузер Chrome, изолированный localhost:4841: уровень 9 получил отказ у прохода, уровень 10 вошёл в снег и вернулся, региональное имя/мини-карта обновились. Все четыре экспортированные GLB открылись в бестиарии; проверены выбранные клипы шага/атаки. Игровая сцена показала големов в движении и ближний контакт, деревья/тени/снег. Исправлены однотонность поверхности, резкий белый край у лесного входа и контраст заголовка/кнопок C/I. Итоговый кадр `artifacts/account-qa/snow-final-surface.png` осмотрен координатором; `snow-lynx.png`, `snow-yak.png`, `snow-spider.png`, `snow-golem-preview.png` — остальные локальные свидетельства. Ошибок консоли в игре/бестиарии не зарегистрировано. Точный радиус попаданий и центральная карточка HP обеих элит визуально не подтверждены; их конфигурация/редкость проверены кодом и тестами. Стенд оставлен на выборе тестовых героев, обычных аккаунтов в нём нет.

# Google-аккаунты и выбор героев — 16 сентября 2026

На объединённом локальном коде `npm run check` прошёл (60 JavaScript-модулей и строгий TypeScript), `npm test` — **233/233**, без пропусков. Проверены подписанные RSA Google JWT (issuer/audience/nonce/expiry/email), принадлежность героев, миграция schema 5, конкурентное создание ровно пяти героев, одно игровое подключение на аккаунт, отзыв/истечение cookie, AFK без команд ввода и гонки переподключения/выхода во время записи. Сетевые и миграционные сценарии используют временную настоящую PostgreSQL, без гостевого HTTP-входа в обычном приложении.

Промежуточные запуски выявили гонки входа с отложенным удалением героя и тестовые запросы во время штатной краткой недоступности записи. Сервер резервирует аккаунт до асинхронной работы и повторно проверяет сессию после записи. При запуске всех файлов одновременно сетевые/SQL-проверки конкурировали за ресурсы и достигали двухсекундного ожидания подключения; runner ограничен четырьмя файлами, внутренняя конкурентность тестов сохранена. Полный итоговый прогон занял 23,2 с; PostgreSQL production-таймауты не увеличивались.

Браузерная проверка на отдельном localhost:4841 с fixture-cookie и отдельной PostgreSQL: вход без настроенного Google, пустой список, создание до пяти, запрет шестого, выбор класса/героя, возврат в список, отказ второй игре на аккаунте, выход и изоляция другого аккаунта. Скриншоты сохранены в исключённом `artifacts/account-qa/`. Реальный OAuth Google не настроен и не проверен; узкий viewport не подтверждён (браузер не применил эмуляцию). Общий 4732 и production не перезапускались, обычные БД не мигрировались. Push/deployment не выполнялись.

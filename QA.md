# Проверка общей 3D-опушки

Обновлено 15 сентября 2026. Основная проверка — браузер, сервер и сохранения. Проба Godot удалена из рабочей папки; её результаты ниже сохранены как история.

Текущий набор — 59 тестов. Последний проверенный игровой релиз — `20260915-ts-01` (`8f9c931`); более ранние результаты ниже отмечены как история. Общая точка продолжения: [STATUS.md](STATUS.md), координация задач в одной папке — [WORKFLOW.md](WORKFLOW.md).

## Одна рабочая папка и удаление Godot — 15 сентября 2026

Локальный `main` в `/Users/igorbelogubov/Projects/AshenFrontier` обновлён до `41e3f59` через fast-forward: TypeScript, характеристики и стенд FPS сохранены вместе со всей историей. Дополнительный `AshenFrontier-character-stats` удалён; `git worktree list` показывает одну папку. AGENTS/WORKFLOW и связанные документы теперь направляют все чаты в неё.

Перед переносом оба локальных игровых сервера штатно остановлены. Внешний архив `/Users/igorbelogubov/Backups/AshenFrontier/consolidation-20260915-091423.tar` содержит исходное состояние обеих папок без зависимостей, `dist/` и Git-метаданных: SHA-256 проверен для 1838 файлов. В общую папку перенесены и сверены 39 файлов браузерных отчётов. Архив и его manifest имеют права 0600 и в Git не входят.

Объединены 11 героев: пять из основной папки, три из рабочей копии и три из эксперимента. Ключи и ID не пересекаются, исходные записи перенесены без перезаписи. Для шести V3-героев проверено точное восстановление всех постоянных полей; для пяти прежних записей — сохранение идентичности, уровня, опыта и золота при штатной миграции. После запуска сравнение с архивом подтверждает сохранность прогресса, вещей, экипировки, распределённых статов и квестов всех 11 героев.

Из рабочей папки удалены проект, скрипты, npm-команды, кеши и экспорты Godot. Сканирование всех файлов, включая игнорируемые и зависимости, без `.git` не находит путей Godot, `.gd`, `.tscn`, `.tres` или `.pck`. Ссылки Markdown на локальные файлы проверены: отсутствующих целей нет. Исторический эксперимент доступен через Git и внешний архив.

В единственной оставшейся папке выполнены чистый `npm ci`, строгая проверка TypeScript, синтаксис 16 JavaScript-модулей и **59/59 тестов**, включая сеть и сохранения. Сервер на 4732 запущен из этой папки; `/health` сообщает `ok: true`, версию сохранений 3 и подключённого игрока. После reload в браузере восстановился прежний маг: уровень 1, 8 золота, 12/65 XP, четыре свободных очка; сцена и HUD отображаются. Нового сравнения FPS не проводилось. Push, deployment и нативные сборки не выполнялись.

## Архивная проба Godot — 14 сентября 2026

Отдельный клиент в `godot-prototype/`, Godot 4.7.2.stable.official, Compatibility. Использует существующие GLB/протокол 2, отдельный Node backend 4733, Web 4741, данные `data/godot-prototype/`. Основная игра 4732 не заменялась. 15 сентября исходники, инструменты и сборки этой пробы удалены из рабочей папки; описание ниже — исторический результат, команды больше не доступны. Исходный код остался в коммите `15a7b9e`.

- `npm run check`: 23 JavaScript-модуля. `npm test`: 52/52.
- Импорт пяти канонических ассетов и release-экспорт Web/macOS выполнены. Исходные Blender/GLB и серверная симуляция не менялись.
- `npm run godot:test`: реальный временный сервер, отдельная сессия Godot, семь мобов, семь клипов героя, изменение реальной позы стопы во время ходьбы. Проверены удержание/отпускание ЛКМ, отсутствие движения/поворота от наведения и WASD/стрелок, остановка над HUD и при потере фокуса, Shift, фиксированный ракурс и колесо.
- Тест проходит путь к волку, удерживает атаку до серверной награды, затем проверяет переподключение и полное пересоздание клиента с чтением записанного ключа. Сохранённые XP/золото и идентичность совпадают. Headless-результат не является измерением графики.
- Регрессия материалов: вершинная окраска `Layered_Fur` включена у всех животных. Отдельно проверяется триангуляция жидкости колб на 10 001 значении от 0 до 1; при почти нулевом HP устранён вырожденный полигон.
- Web просмотрен в живом браузере: читаемые панели, красная HP слева, синяя MP справа, персонаж и окрашенные звери, движение удержанием, бег, приближение, анимация удара, телеграф и потеря здоровья. Убийство волка выдало **12 XP и 8 золота**. После смерти герой вернулся в лагерь. После штатного рестарта тестового сервера и reload клиента эти награды остались.
- Отдельное macOS-приложение запущено и просмотрено. Web и macOS одновременно показывали **двух игроков**; это проверка совместимого входа и общего мира, не испытание вместимости.
- Web-наблюдения менялись примерно от **17 до 60 FPS**. Один замер после прогрева дал p95 переданного в `_process` delta **16.67 мс**, viewport Godot 960×937, два игрока и семь мобов. В ранней нативной сессии при параллельной работе клиентов/захвате окна p95 этого delta достигал **144.44 мс**. Это показания игрового цикла, не GPU-профилирование или независимое измерение времени кадров. Причина разброса не изолирована; нативная производительность не подтверждена стабильным замером.

Окружение, освещение, шейдеры, разрешение и обработка анимаций отличаются от Three.js. Эти результаты **не доказывают превосходство одного движка**. Предсказание под задержку сети, все три класса в интерфейсе, статы/инвентарь, чат, мобильные платформы и нагрузка не проверены в Godot. Для решения о миграции нужен отдельный сопоставимый прогон. Push и deployment не выполнялись.

## Автоматические проверки — 14 сентября 2026

На Node 24: `npm run check` проверяет синтаксис 19 JavaScript-модулей; `npm test` — 52 успешных теста.

- Миграция трёх классов: личность, уровень, деньги и вещи сохраняются, позиция заменяется лагерем новой карты.
- Версия 3 сохраняет здоровье, ману, распределённые очки, ревизию, координаты, откаты, смерть и переполненные награды. Миграция версии 2 сохраняет 3D-прогресс и выдаёт соответствующий уровню бюджет однократно.
- Сервер ограничивает скорость, столкновения, дальность, частоту атак и поддельные пакеты; просроченный ввод останавливается.
- Босс общий, два участника получают личный прогресс/добычу; содержимое чужих рюкзаков и секреты не раскрываются.
- Квест не выдаёт награду повторно; шесть слотов, продажа и полная сумка сохраняют целостность вещей; попытки смены класса не меняют героя, статы или снаряжение.
- Стрелы и магия имеют серверные траектории/столкновения; мобы предупреждают об атаке и возвращаются домой.
- Реальные HTTP/WebSocket-клиенты: движение видно другому игроку, дублирующий/неверный ключ отклоняется, совместный бой, переподключение и штатный рестарт сохраняют данные.
- Закрытые файлы и старый клиент возвращают 404; GLB имеет правильный Content-Type, чужой Origin отклоняется.
- GLB героя/животных: структура скелетов и клипов, конечные координаты, независимые экземпляры, циклы походки и фазы атаки.
- Отдельная регрессия: числовая сила удара не подменяет объект атаки и не ломает предсказание позиции героя.

## Неизменяемый класс героя — 14 сентября

Класс выбирается только при создании. В локальной ветке `codex/character-stats` удалены выбор класса у костра и выдача нового оружия/возврат очков при его смене. Прежняя команда `class` отклоняется сервером. Исторические сценарии смены класса ниже относятся к предыдущей версии.

На Node 24 проходят **52/52** теста и проверка синтаксиса **19 модулей**. Обновлённые тесты проверяют запрет для всех трёх классов в лагере, вне лагеря, при смерти/атаке/бое и полной сумке, сохранность всех постоянных полей героя, классового оружия и вложенных очков. Реальные WebSocket-сессии подтверждают отклонение команды и приоритет сохранённого класса над `classId` при входе по ключу, переподключении и после штатного рестарта, включая героя версии 2.

Локальный сервер 4732 штатно перезапущен. Сравнение сохранённого прогресса до/после подтвердило неизменность классов, уровней, XP, золота, вещей, экипировки, статов и квестов всех трёх локальных героев. В браузере у существующего мага раскрыта «Подготовка у костра»: доступен сброс статов, выбора класса нет, прогресс восстановлен. Ошибок/предупреждений в журнале вкладки нет; `/health` отвечает успешно с версией сохранений 3. Push/deployment не выполнялись.

## Колбы HP и MP в нижнем меню — 14 сентября

Локальная ветка `codex/character-stats`, preview на порту 4732. `npm run check`: **19 модулей**; `npm test`: **52/52**; `git diff --check` без замечаний. Новые тесты игровой логики не добавлялись: изменение относится к представлению текущего состояния.

В браузере проверены маг на обычном размере 740×723 и отдельный QA-воин с кнопками обоих видов оружия, широкая раскладка 1280×720 и узкая 390×844. Красная колба расположена слева, синяя справа от кнопок, круги не деформированы; горизонтального переполнения нет. Открытие/закрытие характеристик сохраняет доступность нижних кнопок, колбы скрываются на время окна. После вложения пяти очков в энергию тестового воина максимум маны вырос с 40 до 60 без мгновенного заполнения: в колбе видны свободное место сверху, текущие 47/60 и последующая регенерация (следующий DOM-снимок — 49/60, заполнение 82,68%). Полный запас здоровья/маны также проверен. Уровень жидкости использует дробное значение, число запаса округляется как прежде.

В журнале QA-вкладки нет ошибок/предупреждений. Настройка размера сброшена, QA-вкладка закрыта, исходная игра пользователя обновлена. Серверные правила и сохранения не менялись, push/deployment не выполнялись.

## Управление удержанием ЛКМ — 14 сентября

Проверено локально на `http://127.0.0.1:4732/` в ветке `codex/character-stats`. Синтаксис **19 модулей**, полный набор **52/52** и `git diff --check` проходят. Пять новых тестов проверяют текущую сторону курсора, одинаковую скорость по направлениям, отсутствие движения/поворота при наведении без удержания, торможение после отпускания, зону остановки у ног, невалидную точку/смерть, дистанцию атаки и прекращение преследования.

В браузере выполнены перетаскивание с зажатой ЛКМ по земле, смена стороны курсора, отпускание над сценой и над кнопкой интерфейса, нажатия WASD/всех стрелок, клик по миникарте, открытие/закрытие окна героя. Отдельный WebSocket-наблюдатель подтвердил движение браузерного героя во время жеста и нулевую скорость после отпускания; после WASD/стрелок и миникарты координаты и угол не изменились. В заключительных 98 снимках за 5 секунд координаты, угол и скорость оставались постоянными. В журнале вкладки нет ошибок/предупреждений. Наблюдатель отключён, игра оставлена открытой.

Свободное наведение без кнопки проверено автоматическим тестом логики ввода; отдельного действия hover у используемого браузерного инструмента нет. Длительное удержание в бою и отпускание вне окна браузера в этой ручной проверке не воспроизводились. Захват указателя, потеря фокуса/соединения и открытие интерфейса сбрасывают удержание в коде. Подсказки обновлены, точечный маршрут удалён. Push и deployment не выполнялись.

## История: ручные характеристики и новый инвентарь — 14 сентября

Локальная ветка `codex/character-stats`, отдельный сервер `http://127.0.0.1:4732/`, собственный каталог `data/`. Push и deployment для этой работы не выполнялись. После изменений полный набор **47/47** проходит на Node 24, синтаксис 18 модулей и `git diff --check` проверены.

Добавлены проверки четырёх характеристик/трёх классов, начального и уровневого бюджета, реального повышения уровня после убийства, повторных/устаревших/поддельных пакетов, атомарности, ограничений лагеря и боя, сброса/смены класса без лечения, переполненной сумки, миграции версии 2, восстановления версии 3 без повторной выдачи маны, попаданий/промахов ближних и дальних атак, расхода маны, регенерации и уменьшения входящего урона. Отдельный тест с настоящими WebSocket-соединениями проверяет старого героя, распределение, приватность, переподключение и штатный перезапуск.

В браузере проверены:

- Воин: черновик +1 силы/+1 живучести показывает урон 26 → 26,8 и HP 100 → 104; применение тратит два очка, сервер подтверждает, обновление страницы сохраняет результат. Бесплатный сброс возвращает бюджет.
- Лучник: +1 ловкости показывает одновременное изменение урона, точности и защиты; отмена убирает черновик. Маг: +1 энергии повышает урон, максимум маны на 5 и её регенерацию на 0,075/с. Смена класса возвращает вложенные очки.
- Фокус кнопки сохраняется при обновлениях мира. `Q` и `W` при открытом окне не вызывают боевую команду и движение. Кнопки действий учитывают соединение, лагерь и состояние героя.
- Компоновка при фактических CSS-размерах 1067×750 (две панели), 740×723 (вкладки) и 325×703 (компактный режим) проверена. На узком экране нет горизонтального переполнения; панель прокручивается, подтверждение и закрытие доступны. Настройка размера браузера после проверки сброшена.
- Маг в общей локации победил волка, получил 12 XP, 8 золота и посох. Смерть/возрождение и обновление страницы сохранили награды и распределение. Новый инвентарь позволил выбрать «Посох сумерек +5», надеть его у костра; надетую вещь нельзя продать. Видны ровно шесть слотов и 16 ячеек сумки.
- Исправлены перекрытие имени героя чатом после добавления маны, видимость пустых ячеек и точность чисел предпросмотра. В журнале просмотренной вкладки нет ошибок и предупреждений.

Таблица формул для одинакового бюджета на уровнях 1/10/20/60 выявила и помогла убрать чрезмерный рост защиты лучника. Сводка и ограничения расчёта — [PROGRESSION.md](PROGRESSION.md). PvP, AFK и длительное испытание баланса ещё не проводились; рост FPS этой работой не заявляется. Модели и анимации не менялись.

## История: браузерная проверка перехода на общий 3D-мир

Два независимых профиля в общем мире: воин «Дозорный» и маг «Искра». В обоих видны второй персонаж и счётчик двух игроков; перемещение синхронизировано. Маг вышел из лагеря, убил волка, получил 12 опыта, 8 золота, 1/5 задания и «Посох сумерек +5»; надел его у костра. Обновление страницы и остановка/запуск сервера восстановили того же мага с теми же наградами и надетым посохом. Соединения восстановились автоматически. Смерть и возвращение в лагерь не обнулили прогресс.

Проверены широкий и узкий браузерные размеры, интерфейс шести слотов и прокручиваемого рюкзака. После устранения ошибки состояния атаки герой виден, новые ошибки рендера не появляются. Около 59–60 FPS в проверенной сцене с двумя героями — наблюдение, не нагрузочная гарантия.

## Публичная проверка и её история

Для проверки HTTPS/WSS используется `node scripts/smoke-online.mjs <адрес>`: два новых QA-героя, общая видимость, движение и серверная запись. Ключи сохраняются только в закрытом `artifacts/shared-3d/online-heroes.json`. После штатного рестарта только игрового сервиса:

```sh
node scripts/smoke-online.mjs https://ashen-frontier.91.99.21.123.sslip.io --restore
```

Сравниваются id, класс, уровень, опыт, золото, вещи, экипировка, координаты и задача. Эта команда проверяет сохранения текущего протокола, не старый пиксельный мир. При первом 3D-релизе публичные HTTPS/WSS и восстановление двух героев после рестарта проверены успешно. Браузер загрузил 3D-опушку и переподключился; сохранились все 11 исходных героев. К последнему релизу `20260914-3d-03` записей было 15, их основные поля прогресса также сохранились. Девять других контейнеров не менялись, шесть прежних HTTPS-адресов отвечали 200. Подробности в [DEPLOYMENT.md](DEPLOYMENT.md).

В новом clone файла с QA-ключами нет. `--restore` требует ранее созданного закрытого `artifacts/shared-3d/online-heroes.json` для того же адреса. Обычные `npm test` не требуют ключей и не подключаются к production. Отчёты из `artifacts/` остаются локальными; проверяемые сценарии и результаты описаны здесь.

## Не покрыто текущей проверкой

Длительная нагрузка с 16 игроками, слабые устройства, аварийная потеря VPS, AFK-начисления, PvP/PK, длительный баланс ручных сборок, рынок и платежи. Мобы и чат перезапускаются вместе с миром. Сохранения JSON с двухсекундным интервалом ещё не заменяют транзакционную экономику. Аккаунты с восстановлением доступа и отдельные клипы дальних классов предстоит сделать; модели трёх классов добавлены 15 сентября (проверка ниже).

## История: исправление свободной прогулки — 14 сентября, 3d-02

Причина рывков была общей для старых процедурных и новых GLB-мобов: патруль следовал за движущейся точкой с порогом остановки 0,12 м. В двухминутной симуляции без игроков получалось 821–876 переходов движение/остановка в минуту на моба, с типичным рывком около 0,065 с.

После замены на постоянную цель, разгон/торможение, поворот и полноценную паузу: 15,5–21 переход в минуту, непрерывная ходьба в среднем 3,9–5,7 с. Отрезков ходьбы короче 0,2 с нет. Все семь мобов остаются в разрешённой области вне лагеря; агрессия прерывает патруль немедленно, скорость погони сохранена, смерть сбрасывает маршрут. Два регрессионных теста добавлены, полный набор 29/29 проходит. В браузере проверены прогулка у лагеря и отсутствие ошибок рендера. Геометрия и клипы в этом исправлении не менялись.

## Доработка волка — 14 сентября

Обычный волк пересобран в Blender, кабан и вожак совпадают с прежними файлами по SHA-256. У волка 24 кости, 18 154 треугольника, восемь клипов. Все 31 тест проходят: дополнительно проверены опора четырёх лап в шаге, рыси и двух поворотах, замыкание циклов и непрерывность смешанной позы при 30/60 FPS. Проверки атаки, смерти, возрождения, сети и сохранений также проходят.

В бестиарии проверены переключение «до / после» в одинаковой фазе шага и воспроизведение поворота. В локации проверены загрузка нового волка и патрулирование, ошибок рендера нет. В этой сессии браузер показывал около 23–28 FPS в локации; это отдельная проблема производительности, улучшение FPS этой правкой не заявляется. Отчёты и журнал сборки: `artifacts/wolf-polish/`. Подробности модели и ограничения: `art/creatures/ashen-fauna-v1/README.md`.

## Проверка репозитория и параллельной работы

GitHub Actions (`.github/workflows/ci.yml`) запускает `npm ci`, `npm run check`, `npm test` на Node 24 для PR и push в `main`. Workflow не использует production-данные и не выполняет deployment. Для локальной проверки после clone достаточно этих же команд: нужные GLB и отчёт сборки воина входят в Git.

Для ручной проверки используется общий сервер 4732 из единственной рабочей папки `AshenFrontier`. Автоматические и нагрузочные проверки используют отдельные временные данные; второй процесс с общим `data/` не запускается. Проверка в разных вкладках одного сервера использует разные QA-сессии. В отчёте обновляются результаты именно текущей задачи; исторические числа тестов и FPS не подменяют новый результат.

## TypeScript — 15 сентября 2026

Основной игровой код проверяется с `strict`, `noEmitOnError`, `verbatimModuleSyntax`. Клиент, сервер, общие формулы и контракты компилируются в `dist/`; исходники не выдаются HTTP. Все прежние 52 теста проходят на собранных модулях, включая настоящие WebSocket-сессии, миграции V2/V3, неизменяемый класс и сохранение после перезапуска. Перенос языка сам по себе не является доказательством ускорения.

## FPS и повторяющиеся скелеты — 15 сентября 2026

`npm run check` и 59/59 тестов: добавлены проверки длинных зависаний в статистике, закрытости диагностических API, изолированной нагрузки 16 настоящих WebSocket-сессий и очистки ботов. Реальный GLB проверен на идентичность деформированных вершин всех семи клипов после объединения одинаковых скелетов; два героя сохраняют независимые кости/текстуры, разные inverse bind matrices не объединяются. При уходе удалённого героя освобождается его GPU-текстура скелета.

Проведены браузерные измерения в Chrome с аппаратным WebGL2/Metal на M1; полная таблица и ограничения в [PERFORMANCE.md](PERFORMANCE.md). Графика и управление проверены в браузере. TypeScript-миграция не меняет схему V3/протокол2. Push, deployment и новые нативные сборки в этой задаче не выполнялись. Dockerfile переведён на два этапа, но локальный Docker отсутствует, образ не собирался.

Основной локальный сервер перезапущен на 4732; существующий профиль восстановился, окна характеристик и инвентаря с шестью слотами открываются. Отдельный нагрузочный сервер 4742 после замеров остановлен. Встроенный браузер в своей меньшей сцене показывал до 60 FPS; это индикатор другого окна/ракурса, не результат сопоставимого стресс-прогона Chrome 1527×810.

## Hetzner: 20260915-ts-01 — 15 сентября 2026

В production выложен commit `8f9c931` после успешного GitHub CI и 59/59 тестов внутри нового Docker-образа. Все 15 прежних героев прошли предварительную миграцию в памяти; защищённая копия снята после штатной остановки старого контейнера. После обновления 13 основных полей прогресса всех этих героев совпали, два прежних QA-профиля восстановили 28 полей V3 по публичному WSS после контрольного рестарта.

Публичные 12 ресурсов совпали с проверенной сборкой, приватные/диагностические маршруты возвращают 404. В браузере проверены вход, общий мир с другим игроком, модели, колбы, четыре стата, пять начальных очков и шесть слотов инвентаря. Добавлен один отдельный браузерный QA-герой: всего при проверке 16 записей. Это проверка релиза, не стресс-тест и не доказательство стабильных 60 FPS.

Caddyfile, ID и время запуска девяти остальных контейнеров совпадают до/после; все шесть соседних HTTPS-адресов и игра отвечают 200. `current` переключён после проверки. Образ healthy, схема 3, обычное хранилище, `GAME_STRESS` выключен. Подробности, резервная копия и ограничения отката V3 → V2: [DEPLOYMENT.md](DEPLOYMENT.md). Отчёты — `artifacts/deploy-20260915-ts-01/`, вне Git.


## Первая случайная экипировка воина — 15 сентября

Локальная функциональная проверка, не production deployment. `npm run check` и `npm test`: 66 тестов. Новые проверки покрывают включённые границы/шаг случайных значений, множество разных экземпляров, поддельные свойства, сохранность старых/новых вещей и pendingItems, ограничения класса/костра, доступность шести слотов, отсутствие двойного бонуса power, влияние скорости на длительность без сокращения отката. Реальные WebSocket-клиенты проверяют надевание, appearance у наблюдателя и те же значения после рестарта временного сервера.

На экспортированном модульном GLB проверены семь клипов, конечные вершины/границы в стойке, ходьбе, беге, обоих ударах, попадании и смерти, тяжёлый/лёгкий/смешанный/снятый комплект, отдельная палитра другого героя и одна общая палитра всех частей одного героя. Неизвестные head/boots appearance дают базовые части. Позиция головы в Idle через 4 секунды совпала с исходным GLB.

В браузере проверены смена облика в примерочной, бег, новый образец и максимум HP +15 в диапазоне 5–15. Примерка не соединяется с игровым WebSocket и не выдаёт вещи герою. В обычном клиенте изолированного сервера проверены диапазоны/сравнение, прокрутка к описанию, надевание кожаного доспеха и отметка «Надето»; максимум HP изменился с 139 до 136 при замене +13 HP на +10 HP. После перезагрузки клиента значения сохранились. Обычные сохранения не использовались как тестовая база.

Короткий FPS-стенд: M1, Chromium, 1280×720/DPR 1; 5 секунд прогрева + 12 секунд измерения. Смешанное снаряжение 1/4/16 воинов: 60/60/60 FPS, p95 17,6/17,6/18,5 мс, кадров >50 мс нет. Контроль 16 воинов с прежними предметами в том же модульном ассете: 60 FPS, p95 17,7 мс. [Конфигурация и счётчики](docs/equipment-performance-2026-09-15.json). Это не сравнение с прежним бинарным ассетом и не доказательство длительных 60 FPS, производительности любого устройства или вместимости production.

Ограничения: десять пробных основ только воина, без сетовых бонусов/заточки; баланс всех классов не настроен на эти вещи. Тело/голова под снятой бронёй пока упрощены, у кожи есть пересечения с унаследованными пластинами при некоторых позах. Источник/генератор/экспорт сохранены; дальнейшая художественная полировка отдельно.

Общий локальный сервер 4732 перезапущен штатно при нуле подключённых игроков после резервного копирования. Содержимое сохранений до/после перезапуска идентично; `/health` успешен, новый GLB отдаётся с HTTP 200. Изолированный сервер 4742 остановлен после проверки, примерочная оставлена на 4732. Production и остальные сайты не затрагивались.


## Mixamo: три модели и замена кожи — 15 сентября 2026

Локальная версия после первой примерки. `npm run check` успешен, `npm test` — 69/69. Три новых проверки используют реальные экспортированные GLB: отдельные текстурированные тела лучника/мага, одна палитра 67 костей на героя, независимые палитры клонов, семь клипов с конечными границами в девяти фазах и человеческим масштабом Idle. Положение вершин тяжёлых Armor_Body/Helmet/Boots совпадает с архивным Paladin; под кожаным капюшоном остаётся новая голова Exo Gray с UV.

Браузер Chrome: проверены Exo Gray без снаряжения, тяжёлый комплект, новая кожа и бег; Erika Archer и Dreyar в примерочной, выбор класса и соответствующие отдельные модели в общей изолированной локации. При подготовке обнаружен десятикратный размер исходного скелета Dreyar — исправлена нормализация единиц в Blender, повторно проверен именно GLB. Классовый выбор в примерочной не меняет героя или его вещи. Класс обычного игрового клиента берётся из серверного профиля до загрузки ассета.

Четыре коротких замера на `npm run stress`, порт 4742, временные данные: 1 герой — 57 FPS; 16 героев трёх классов — 28,1 FPS в первом прогоне и 41,8 FPS при повторе; без теней — 43,7 FPS. Первый полный прогон содержит кадр 2182 мс. Условия и числа сохранены в [PERFORMANCE.md](PERFORMANCE.md) и [JSON](docs/mixamo-models-performance-2026-09-15.json). Стабильные 60 FPS в толпе не подтверждены. Это другой браузерный режим/разрешение относительно предыдущей примерки; прямое сравнение старых и новых моделей не проведено.

Ограничения: стрельба и каст пока используют прежние временные клипы; лук/посох ещё процедурные. Erika и Dreyar пока имеют цельную базовую одежду. Новая кожа воина подогнана к общему скелету, отдельные пересечения в крайних позах ещё возможны. Лицевая анимация Exo не используется. Реалистичный персонаж не означает окончательно согласованный арт всех классов.

Статические клиентские ресурсы обновлены на общем локальном 4732 без перезапуска игрового сервера. Обычные сохранения и production не менялись. Стресс-сервер 4742 остановлен, временный размер окна браузера сброшен, примерочная оставлена на 4732. Push/deployment не выполнялись.

## Иконки предметов и отдельный рюкзак — 15 сентября 2026

`npm run check` успешен; финальный `npm test` — 74/74. Новые проверки: надевание/снятие не теряют и не дублируют экземпляр; 16 ячеек считаются отдельно от экипировки; полный рюкзак блокирует снятие, но допускает замену; надевание в пустой слот освобождает место для одной ожидающей награды; предмет в последней свободной ячейке не помечается ожидающим. Проверены наличие всех 15 прозрачных PNG 256×256 и отдельная картинка каждой основы воина. Расширена проверка настоящих WebSocket-клиентов и рестарта: надетые исключены из рюкзака, снятое возвращается один раз, свойства и все экземпляры совпадают.

Один промежуточный полный запуск упёрся в прежний 9-секундный таймаут боя с боссом в shared-server; финальный полный прогон после закрытия браузерной тестовой сцены прошёл. Боевой тест и его таймаут в этой задаче не изменялись.

Браузер Chrome, изолированный сервер 4743 и временные герои: шесть вещей в рюкзаке при шести надетых. Замена панциря на кожу сохраняет 6/16, кожа уходит из сетки, панцирь возвращается в неё. Снятие кожи даёт 7/16 и пустой слот доспеха. После перезагрузки остаётся 7/16; та же кожа встречается в рюкзаке ровно один раз. Проверены реальные PNG в слотах/рюкзаке/описании, лук/посох чужих классов, отсутствие ошибок браузера и загрузка каждого показанного изображения. Иконки осмотрены также в примерочной на 4732. Исходные модели/анимации не менялись; отдельный FPS-замер не проводился.

Общий локальный сервер 4732 перезапущен штатно при нуле игроков после резервного копирования. SHA-256 обычного файла сохранений идентичен до/после, health успешен; HTTP-ответы проверенных PNG совпадают с локальными файлами. Изолированный сервер 4743 остановлен, его вкладка закрыта. Все тестовые вещи находились во временном хранилище, обычным героям ничего не выдавалось. Схема сохранений по-прежнему V3. Push и deployment не выполнялись.


## Общая интеграция экипировки, навыков и спотов — 15 сентября

`npm run check` успешен, `npm test` — **109/109** на объединённой версии. Ветки агентов (`codex/class-equipment`, `codex/class-skills`, `codex/forest-spots`, `codex/combat-review`) объединены локально в `main`. Тестовые ключи/сохранения и FPS-отчёты не публикуются; production не использовался.

Проверены серверные ограничения шести навыков, урон/предел целей/точность/препятствия, MP/откаты и отсутствие повторного попадания снаряда. Настоящие WebSocket-пакеты передают публичный каст и эффект второму игроку; приватные мана/откаты и существующие вещи переживают перезапуск временного сервера. Новый каталог каждого класса даёт шесть последовательных слотов, сохраняет точные броски и публикует только appearance другим игрокам. Переполнение рюкзака, переодевание и наследованные предметы покрыты регрессиями.

AFK проверен через World и реальные соединения: старт только на споте, нейтральный ввод не отменяет, ручное действие/смерть/разрыв соединения отменяют; после загрузки режима нет. Автоматический вклад не выполняет сюжет, очередь ограничивает дальнейшую автоохоту, мобы остаются в споте и возрождаются через 16 секунд. 60-секундная симуляция стартовых вещей показывает риск смерти — детали в docs/AFK.md, это не доказательство долговременного равенства классов.

Реальные GLB экипировки и библиотеки Mixamo проверены во всех шести навыках (61 фаза): конечные вершины, отсутствие ухода корня/взрыва меша, сброс после атаки/смерти. Браузерная мастерская показала натяжение лука со стрелой, выпуск, одноручное и круговое заклинания. Дополнительная проверка трёх GLB мобов покрывает все клипы и 25 фаз, включая выпад/смерть, внутри постоянных границ отсечения.

В Chrome проверена сцена с новыми Q/E, расходами MP и отдельными откатами, а также правый клик поверх открытого инвентаря: браузерное контекстное меню не появляется. Исправлен найденный визуальный дефект: пул эффектов больше не поглощается объединением статического окружения, белого кольца у лагеря нет. Два лесных спота и четыре коллекции дополнительно осмотрены агентами в отдельных браузерных мастерских; их доказательства и ограничения описаны в docs/FOREST_SPOTS.md и docs/CLASS_EQUIPMENT.md.


На временном стенде браузер показал «Автоохота включена», текущие HP/MP и круг спота. Кнопка остановила режим, отдельные Q/E списали MP и показали свои откаты (3,8 с / 4,8 с сразу после нажатия), F снова включила автоохоту. Автоматические убийства оставляли сюжет 0/5; ручное убийство после остановки увеличило его до 1/5. Воин с шестью тестовыми предметами виден в самом споте; границы деревьев/камней и телеграфы не пропадают. Широкий HUD расширен для новых кнопок, на узких экранах сохраняется перенос.

Общий сервер 4732 штатно перезапущен при нуле подключённых/живущих в мире героев. До этого сделана игнорируемая резервная копия; после запуска **все 12 записей локальных героев в точности совпали с копией**, `/health` — ok, V3. Это проверка локальной установки, без отправки изменений на GitHub/Hetzner. FPS-стенд и дополнительный сервер 4744 после проверки остановлены; полные числа, включая плохие повторные результаты плотной толпы, сохранены в PERFORMANCE.md.

## PostgreSQL, открытый мир и интерфейс — 15 сентября 2026

Интеграционный набор после карты, добычи/торговца и хвата лука: `npm run check`, `npm test` — 136/136. Тесты используют отдельные временные базы PostgreSQL. Проверены реальные WebSocket-покупки/продажи, точные предметы и сохранение после рестарта, ручной подбор, владелец добычи/препятствия/полный рюкзак, четыре спота, immutable rolls, ревизии и идемпотентность DB-пакетов. Принудительное отключение writer-сессии даёт 503 и закрывает игровые соединения; неподтверждённая продажа не публикуется, рестарт восстанавливает последний commit. Исправлена изоляция теста advisory lock: выбор PID ограничен OID собственной базы, иначе параллельный тест мог завершить чужой writer.

Браузер Chrome, изолированный сервер 4734 и временные герои:

- При LMB по подписи NPC сервер открыл магазин и инвентарь. Три вкладки, шесть товаров класса; картинки предметов соответствуют моделям. ПКМ купил меч за 40 (500 → 460), ПКМ в рюкзаке продал его (→ 477). Проверены также покупка посоха перетаскиванием и продажа сапог переносом в магазин.
- ПКМ вне магазина надевает капюшон, он исчезает из рюкзака и появляется в слоте. Перенос капюшона обратно снимает, перенос в пустой слот надевает. Использован pointer drag: исходный HTML drag в Chrome не доводил перенос до пустого слота. Подсказка появляется при наведении/клавиатурном фокусе без выбора; видны все значения, диапазоны и сравнение.
- Рюкзак: 16 ячеек, около 84,6 × 84,6 CSS px на широком экране; белые и зелёные рамки осмотрены. Синий стиль соответствует третьему существующему tier. Размеры окна фактически 1527 × 809 и 745 × 673 CSS px (viewport override масштабируется Chrome). При узком открытом инвентаре C/I не перекрывают ячейки. `artifacts/current-ui/compact-shop.png` — локальный снимок.
- Настоящее убийство временным героем оставило 8 золота на земле, не в кошельке. В браузере LMB по названию запустил подход, кучка исчезла, кошелёк стал 8. Сетевой тест отдельно проверяет сохранение и повторный подбор без дублирования.
- Интегрированный лучник загружен без ошибок консоли: лук в ладони, отдельные классовые иконки. [720 кадров и визуальный осмотр обоих луков](docs/BOW_GRIP.md). Повтор ПКМ проходит через тот же цикл атаки, что Space; сброс удержания предусмотрен на отпускание, уход с canvas, отмену/focus/death/panel.
- Резервный дамп восстановлен в отдельную PostgreSQL-базу; три тестовых героя, предметы/броски/экипировка/золото/ревизии совпадают. Общий сервер 4732 затем перезапущен с чистой PostgreSQL; 8 прежних JSON-файлов заархивированы, контрольные суммы остались прежними. Production не обновлялся.

Результаты FPS записываются отдельно в PERFORMANCE.md. Скриншот обычной игры и прохождение тестов не доказывают стабильные 60 FPS, длительный AFK, слабые устройства или production-нагрузку.

После исправления стресс-подключений финальные `npm run check` и `npm test` прошли: **137/137**, без пропусков. Новый сетевой сценарий выполняет 1 лагерь → 16 AFK → 1 лагерь → 16 AFK на настоящей изолированной PostgreSQL; повторный профиль получает новые ID экземпляров, наземная добыча очищается при смене сценария.

Финальный браузерный стресс-прогон подтверждает 16/16 активных AFK, 16 навыков и 16 атак за 12 секунд: 36,5 FPS; одиночный лагерь — 42,5 FPS. Стабильные 60 FPS не достигнуты; условия, плохие хвосты серверного тика и исключённый ошибочный AFK-сценарий описаны в [PERFORMANCE.md](PERFORMANCE.md). После проверки временные серверы/кластеры остановлены, тестовая вкладка закрыта; общий сервер 4732 остаётся с PostgreSQL и успешным health.


## Production PostgreSQL: 20260915-pg-01 — 15 сентября

По команде пользователя опубликован `96374e9`. GitHub CI успешен, полный повтор 137/137 внутри runtime-образа на изолированном PostgreSQL. Проверены публичный WSS двух героев, взаимная видимость/движение и совпадение 26 полей после штатного рестарта. Одиннадцать ресурсов совпали побайтно; дамп с двумя героями восстановлен отдельно, все семь таблиц совпали по количеству/хешам строк. Caddy и девять других контейнеров не изменились; шесть соседних адресов HTTP 200. Детали, пределы backup и отличие от разрабатываемых четырёх навыков/Стадиума — в DEPLOYMENT.md.

## Четыре навыка, Стадиум и интеграция клиента — 15 сентября

Финальные `npm run check` и `npm test`: **162/162**, без ошибок и пропусков, runner 35,5 с. Полный лог — `artifacts/stadium-integrated-final-test.log`. Включены прежние PostgreSQL/экономика/сохранения и новые проверки классов, попаданий/предела целей, двенадцати GLB-действий, замедления, предупреждений/отложенных областей, молнии, трёх загонов, порталов, сетевой изоляции областей и восстановления сохранённой позиции. Тесты используют временные PostgreSQL-базы, не обычный мир.

Промежуточные прогоны выявили две проблемы и не считаются успешными результатами: проверка HP ожидала ответ зелья фиксированные 80 мс; заменено ожиданием серверного расхода зелья и верхней границей естественного регена. При входе во время асинхронного сохранения сервер иногда молча оставлял сокет без ответа; теперь возвращается `storage_unavailable`, после чего клиент повторяет вход. Новая сетевая проверка синхронизирует это пересечение в тестовой БД, подтверждает отсутствие созданного бесхозного героя и успешный повтор. После уже подтверждённого создания unrelated checkpoint не отменяет выдачу ключа.

В Chrome на изолированном 4734 проверены:

- ЛКМ по подписи портала в лагере → серверный подход → безопасная площадка Стадиума; обратный портал возвращает в лагерь. Меняются заголовок, миникарта/маркеры и подсказки; старая цель/ввод/эффекты не переносятся.
- Вход по сохранённой позиции сразу открывает Стадиум. Во время тестовой смерти лучника камера и интерфейс вернулись в лесной лагерь. Лесной квест не получил зачёт от убийства кабана в Стадиуме.
- Все Q/E/Z/X трёх классов: корректные названия/иконки, запуск клавишами, расход MP и отдельные серверные откаты. У лучника Q/E дали реальный kill/XP и личное золото на земле; Z/X проверены отдельным экземпляром на дорожке. Вверх направленный лук дождя и разные жесты мага/воина видны в игровой камере. Подробные фазы и два физических лука дополнительно проверены в мастерской/GLB-тестах агента.
- Все три загона есть на миникарте; кабанья группа/ворота видны в игре. Полная геометрия трёх загонов/18 моделей проверена отдельным арт-стендом, AFK всех девяти сочетаний класс/загон и повторное появление мобов — серверными тестами.
- При viewport 600 × 850 шесть одинаковых клеток идут в два ряда, измеренная ширина каждой 87 CSS px; колбы стоят по сторонам, C/I доступны, подсказка не выходит за панель. Override снят. Ошибок/предупреждений игровой консоли при проверке нет.

Материалы — `artifacts/stadium-integrated/`; агентские пошаговые изображения — в worktree анимаций. Частота из обычного HUD не считается воспроизводимым FPS-тестом. Длительная нагрузка новой арены и равенство классов в PvP не подтверждены.

После проверки временный сервер 4734 и его PostgreSQL остановлены, база удалена, вкладка закрыта. Общий 4732 штатно перезапущен из main при нуле игроков после `backups/before-stadium-20260915.dump`; health PostgreSQL/writer успешен. Количество и хеши всех строк семи таблиц до/после совпали. Сохранения не заменялись. Новые навыки/Стадиум остаются локальными коммитами, production по-прежнему `20260915-pg-01` (`96374e9`).
